# Live Subtitle + Agent 领域语言

> 本文件只定义项目内的规范术语。功能约束与验收口径见
> [`docs/semantic-contract.md`](docs/semantic-contract.md)。

| 规范术语 | 定义 | 避免使用 |
|---|---|---|
| **字幕系统（Subtitle System）** | 可独立运行的本地核心系统，负责音频采集、实时 ASR、字幕显示、定稿持久化和带时间戳的历史查看；Agent 系统不可用时它仍必须完整工作。 | “Agent 的输入模块”——字幕系统本身就是可交付产品，不是附属管线。 |
| **Agent 系统（Agent System）** | 消费字幕系统已经提交的会话正文，通过 LLM、内容型工具和 Agent Loop 生成上下文增强文本、摘要及办公派生产物的可选系统。首版只生成内容，不执行外部操作。 | “字幕后处理”——它有独立生命周期和失败边界，但不拥有字幕事实；也不是可任意操作系统的编码 Agent。 |
| **字幕提交边界（Committed Transcript Boundary）** | 字幕系统把首次稳定转写持久化成功后，对后续能力开放的单向边界；临时字幕和精修稿不得伪装成新的原始转写事实。 | 直接让 Agent 消费 partial、让派生文本覆盖原始转写，或让 Agent 控制音频/ASR 主循环。 |
| **Agent Loop** | Agent 系统内由 LLM 决策、调用受控工具、观察结果并继续迭代，直至产出或停止的一次有界执行循环。 | 把一次普通 LLM 文本请求也称为 Agent Loop。 |
| **Agent 插件宿主（Agent Plugin Host）** | 位于产品应用层、包裹 Pi 低层 Agent 核心的能力注册与生命周期边界；负责清单、依赖、权限、激活、取消、错误隔离和产物提交。 | Pi coding-agent 的完整扩展运行时；也不是让任意插件直接访问数据库、文件系统或进程。 |
| **Agent 能力插件（Agent Capability Plugin）** | 向 Agent 插件宿主贡献一种窄能力的第一方模块，首版类型包括上下文提供器、内容产物生成器和质量评估器。 | 一个插件同时拥有音频、ASR、数据库和 UI 的“大模块”；任意第三方代码包。 |
| **Agent 模型 provider（Agent Model Provider）** | 为摘要、增强文本、个人记忆和调试聊天提供模型推理的本地或云端服务选择；它与识别 provider 相互独立。 | “AI provider”——容易与权威识别服务混淆；也不得因选择它而改变字幕会话。 |
| **后台 Agent 任务（Background Agent Job）** | 在字幕提交边界之后，围绕固定输入快照异步生成一种 Agent 产物或记忆结果的可恢复工作单元。 | 字幕会话的一部分；阻塞会话停止或应用退出的同步后处理。 |
| **Agent 处理资格（Agent Processing Eligibility）** | 由请求来源、Agent 总开关、终态会话的已提交正文、自动处理时间边界、Agent 模型 provider 配置及云端披露等事实按固定优先级共同决定的闭集结果，表示一个冻结输入是否可以创建或运行后台 Agent 任务。 | 后台 Agent 任务状态；模型就绪状态；用一次 IPC 成败代替持久资格事实。 |
| **专用子 Agent（Scoped Sub-agent）** | Agent 插件宿主为一种固定 recipe 创建的单层、隔离且有界的 Agent Loop，只能使用该 recipe 批准的能力。 | 可传入任意提示词或递归委派的通用 `spawn_subagent`；完整 coding Agent。 |
| **调试聊天（Agent Debug Chat）** | 默认隐藏、供开发与验证使用的基础聊天界面，只围绕用户明确选择的终态会话、个人记忆和受控业务工具工作。 | 面向普通用户的通用助手；自动记忆来源；内部思维过程查看器。 |
| **隔离 Agent 内核开发入口（Isolated Agent Core Development Entry）** | 只在开发环境启动的独立 Agent 验证应用，使用独立 renderer、IPC、进程、数据目录和 SQLite，验证 Agent Loop、能力权限、后台 Agent 任务、恢复与调试聊天；它不启动或接入字幕系统，也不进入正式安装包。 | 正式 Agent 产品入口；字幕系统隐藏入口；已经实现会后结构化纪要、个人记忆或确认关键词。 |
| **个人记忆（Personal Memory）** | 从终态会话正文中筛选并保存在本地的结构化、可追溯上下文，覆盖用户经历的项目轨迹、事实、决定、待办、术语和偏好候选。 | 权威原始转写的副本；无来源的用户画像；把所有正文无差别长期保留。 |
| **记忆候选（Memory Candidate）** | 自动提取但尚未成为明确偏好或确认关键词的带来源条目；它可以在约定范围内低权重辅助 Agent。 | 用户亲自确认的事实；可直接影响 ASR 或覆盖明确内容的偏好。 |
| **确认关键词（Confirmed Recognition Term）** | 用户明确确认、可在未来新会话开始时交给支持该能力的识别 provider 的术语、实体名或别名。 | 自动推断关键词；会后字符串替换；可在活动会话中即时生效的配置。 |
| **字幕上下文插件（Transcript Context Plugin）** | Agent 系统内的只读适配插件，按 `sessionId + inputWatermark + transcriptVersion + digest` 读取权威原始转写或用户明确选择的派生版本，并转换为 Agent 上下文。 | 字幕系统本身；它不启动采集、不运行 ASR、不拥有或改写字幕会话。 |
| **会话（Session）** | 用户一次开始到停止的监听边界，拥有唯一 `sessionId` 和一个固定音频来源。任何字幕、摘要和检索结果都必须归属且只能归属一个会话。 | “录音”——本项目不保存原始音频。 |
| **系统音频（System Audio / `loopback`）** | Windows 当前播放输出经回环采集得到的音频来源，可能包含远端参会者、通知或媒体声。 | “本地声音”“对方声音”——前者含义不清，后者错误暗示单一说话人。 |
| **麦克风音频（Microphone Audio / `mic`）** | 用户选择的物理或虚拟麦克风输入。它表示采集来源，不保证等同于某一个人的身份。 | “我的声音”——除非产品另有身份识别证据。 |
| **监听模式（Listening Mode）** | 一次会话选择且只选择 `loopback`（会议字幕）或 `mic`（个人听写）；两者互斥，运行中不得并发，也不得直接切换来源。 | “双路模式”“同时听会议和麦克风”。 |
| **音频来源（Audio Source）** | 音频进入系统的确定性来源，当前为互斥的 `mic` 或 `loopback`。来源归因不等同于说话人分离。 | “说话人”——当前项目没有承诺 diarization。 |
| **字幕段（Segment）** | 同一会话、同一音频来源下的一段连续语音内容，以 `segmentId` 标识，可随修订形成多个版本。 | “句子”——VAD 分段不保证语言学上的完整句子。 |
| **字幕事件（Caption Event）** | 描述字幕段实时假设或首次稳定转写的事实。精修稿、翻译、摘要及其他派生文本不属于原始字幕事件。 | “字幕记录”——容易误解为可原地覆盖的单行数据；也不要把精修稿或 Agent 产物伪装成 ASR 原始事实。 |
| **临时字幕（Partial Caption）** | 说话过程中不断变化的整段识别假设，只服务低延迟实时界面，不持久化，也不进入历史、摘要或检索。 | “增量新字”“草稿定稿”“可恢复字幕”。 |
| **首次稳定转写（First-pass Final）** | 实时识别在字幕段关闭时给出的第一次 `final`；它是该段不可变的权威原始转写，必须持久化并可独立查看和导出。 | “一定正确的最终文本”——稳定只表示实时识别阶段结束。 |
| **精修（Refinement）** | 用户在会话开始前选择的可选二遍识别过程；默认关闭，产生独立精修稿而不修改首次稳定转写。 | “更高权威修订”“摘要”或“静默纠正原文”。 |
| **精修偏好（Refinement Preference）** | 决定未来新会话是否启用精修的全局用户偏好，不区分 `mic` 或 `loopback`。会话开始时读取一次并在该会话内冻结；修改或关闭只影响以后新建的会话，不改变活动会话，也不删除旧会话精修稿。运行中的精修 worker 故障不得修改该偏好；只有应用启动时发现精修模型缺失或损坏，才把偏好明确回落为关闭。 | 每种音频来源各一套开关；活动会话内即时切换；worker 故障后自动关闭未来会话精修；关闭后清除既有精修历史。 |
| **精修稿（Refined Transcript Version）** | 精修对同一字幕段生成的可选派生版本，必须与首次稳定转写分别保存并允许用户明确切换查看。已经生成的精修稿不会因同会话后续精修故障、全局精修偏好关闭或未来会话使用原始版显示而删除。 | “当前正文”“原始转写”或覆盖 `final` 的新事实。 |
| **精修覆盖不完整（Incomplete Refinement Coverage）** | 有首次稳定转写的精修会话中，精修稿数量少于首次稳定转写数量。`M` 是该会话全部已持久化首次稳定转写的数量，包含因停止收尾、队列限界或精修故障而没有精修稿的段；`N` 是其中已有精修稿的数量，`partial` 不进入任一计数。覆盖度必须按整个会话的权威数据计算并显示为“已精修 N/M 段，M−N 段使用原始版”，不得从当前分页估算。只要 `0 < N < M`，精修视图保留已有精修稿，缺失段回退原始版并统一标记 `[原始版回退]`，且不自动重新精修；允许用户明确导出该混合视图，但文件名必须使用 `refined-incomplete`，txt/md 写覆盖提示，txt/md/srt 都标记回退段。若 `M > 0` 且 `N = 0`，界面显示“本会话未生成精修稿”，禁用精修视图和精修导出；若 `M = 0` 且没有精修故障，则不显示 `0/0` 或精修结果，仍禁用精修视图和精修导出。原始版查看与导出始终不变。 | ASR 的 `partial`；当前 50 条详情页的局部比例；为掩盖停止收尾或队列跳过而缩小 `M`；用没有已定稿字幕的 `0/0` 制造虚假完成感；也不得把全原始正文或混合正文伪装成完整精修结果。 |
| **精修会话结果（Refinement Session Result）** | 按会话持久化的运行事实，至少区分结果是否有当时运行时证据、该会话是否按冻结偏好启用精修、是否发生精修故障，以及按权威数据派生的整场 `N/M` 覆盖。新会话结果有运行时证据；旧版本会话只能标记“未记录精修运行状态”，不得从 `N/M` 反推故障。故障只使用 `REFINE_WORKER_START_FAILED`、`REFINE_WORKER_EXITED`、`REFINE_DECODE_FAILED`、`REFINE_INVALID_RESPONSE`、`REFINE_INTERNAL_FAILURE` 五个稳定错误码；启动时发现模型缺失或损坏不属于会话故障。故障确认时立即写入，终态收束时补齐结果；故障事实与覆盖度彼此独立：`N < M` 不足以证明 worker 故障，`N = M` 也不得掩盖确实发生的故障。结果在应用重启后仍可从历史查看；若故障会话异常退出，历史将其标为中断并继续显示该故障。 | 用当前页估算结果；把“覆盖不完整”等同于“worker 崩溃”；把旧会话无证据解释成“没有故障”；因最终覆盖完整而删除故障事实；只写一次性 toast 或自由文本日志。 |
| **工具条会话状态通知（Toolbar Session Status Notice）** | 精修故障会话正常停止后，在现有工具条 bounds 内显示的一条内联状态信息，例如“精修异常，已精修 73/100 段”，并提供“查看历史”。它不弹系统通知或模态框、不抢焦点、不改变字幕/工具条大小，也不对字幕内容做摘要、提炼或改写。通知保持显示，直到用户点击关闭或“查看历史”；开始下一会话时自动清除，应用重启不重放。 | “工具条摘要”或“会后摘要”——容易被误解为 AI 内容总结；系统通知、弹窗、声音提醒或字幕正文；跨新会话残留或应用重启后重放。 |
| **显示正文（Displayed Transcript Text）** | 字幕、历史或导出当前选择呈现的文本版本；默认是首次稳定转写。会话已启用精修时，迟到精修稿自动更新仍可见的已定稿字幕段，但不得修改当前 `partial`，已逐行淘汰的段也不得复活。精修 worker 故障确认后，所有仍可见的已定稿段立即恢复为各自首次稳定转写并在原固定视口内重新排版；当前 `partial` 原样保留且优先级不变，已淘汰段不重新插入，后续段继续显示首次稳定转写。历史与导出只有在用户为当前会话明确选择时才呈现精修稿；不完整精修视图的回退行必须标记 `[原始版回退]`。 | “权威转写”——显示选择不改变原始事实；静默混合原始版与精修稿；用精修更新或故障回退打断当前实时识别行；把精修故障升级为字幕会话故障。 |
| **核心字幕模型资源包（Core Subtitle Model Bundle）** | 字幕 MVP 默认运行所需的本地资源集合：临时字幕识别器、权威识别器与 VAD；三者共同决定核心字幕能力是否就绪。 | 把可选精修模型算入默认就绪条件；缺少任一核心资源仍开始会话。 |
| **临时字幕识别器（Draft Recognizer）** | 读取与权威识别器相同的单路、有界内存样本，只为当前字幕段提供低延迟 `partial`；不得产生 `final/refined`，也不得写入 SQLite、历史、导出、日志或报告正文。 | “第二路识别”“草稿定稿”或新的音频来源。 |
| **权威识别器（Authoritative Recognizer）** | 读取与临时字幕识别器相同的单路、有界内存样本，负责同段权威 `partial` 接管与唯一首次 `final`；其首次稳定转写继续作为持久化、历史、导出与可选精修的基础。 | “一定正确的识别器”；让临时字幕覆盖首次稳定转写。 |
| **识别 provider（Recognition Provider）** | 为一次字幕会话提供流式识别能力的本地或云端实现；它只承担识别，不等同于 Agent 模型 provider。 | Agent provider；音频来源；监听模式。 |
| **权威识别策略（Authoritative Recognition Strategy）** | 会话开始时冻结的权威识别选择，当前只允许纯本地权威识别或云端主力识别与本地降级。 | “双路模式”“混合模式”——前者违反来源互斥，后者没有说明谁产生唯一首次 `final`。 |
| **纯本地权威识别（Local-only Authoritative Recognition）** | 一次会话完全由本地临时字幕识别器和权威识别器产生字幕及唯一首次稳定转写。 | “离线模式”——它描述识别策略，不承诺 Agent 或模型供给过程没有网络。 |
| **云端主力识别与本地降级（Cloud-primary Recognition with Local Fallback）** | 一次会话正常期间由云端识别 provider 产生字幕和唯一首次稳定转写，连接明确失效后在同一会话内单向改由已就绪的本地识别链路继续。 | “云端唯一模式”；云端与本地同时争抢 `final`；因普通延迟波动来回切换。 |
| **精修模型资源（Refinement Model Resource）** | 仅由未监听状态下独立、明确的下载动作按需供给；缺失时尝试开启精修必须保持关闭并提示下载，安装就绪后也不自动启用。用户可明确取消下载；合法 `.part` 可保留，但应用不得自动恢复网络请求，只有用户再次点击“继续下载”才可续传。已开启偏好仅在应用重启时模型仍通过就绪证明才可保留；若模型缺失或损坏，启动时必须把开关明确回落为关闭并通知用户，重新下载成功后仍需用户再次明确开启。缺失或失败不得阻止核心字幕与原始历史工作。 | “核心字幕模型资源包”的必需组成；由精修开关隐式触发的自动下载；应用重启后自动续传；模型失效后静默保留一个实际不可用的开启状态。 |
| **模型就绪证明（Model Readiness Proof）** | 固定 manifest 对资源 URL、字节数、SHA-256 和运行文件的约束，加上安装目录内严格 ready marker 与普通文件检查，共同形成的本地可用性证明。 | “文件夹存在”“下载到 100%”——这些都不能单独证明资源可信且可调用。 |
| **模型资源 UI 闭环（Model Resource UI Journey）** | 从模型资源缺失开始，由用户在设置页主动触发安装，持续看到受控进度，直至临时字幕识别器、权威识别器与 VAD 组成的核心字幕模型资源包就绪并能开始字幕会话的一条用户旅程。 | “模型安装内核已通过”“资源页预置为 ready”——两者都没有证明用户点击路径成立。 |
| **翻译（Translation）** | 基于某一正文修订生成的派生文本，必须声明 `basedOnRevision`。翻译不会改变原文正文。 | “双语正文”——原文与译文的权威性和版本关系不同。 |
| **增强文本（Enhanced Transcript）** | LLM 根据已提交字幕及上下文生成的独立派生文本，必须声明输入水位、input digest 和模型；它可以改善可读性，但永不替换权威转写。首版按会后或用户主动请求对整场生成，滚动逐段增强后置。 | “精修”——精修仍是字幕系统的 ASR 修订；“修改原文”。 |
| **权威原始转写（Canonical Source Transcript）** | 会话内所有首次稳定转写按来源、字幕段和时间组成的不可变文本视图；它始终可以脱离任何精修稿或 Agent 产物单独查看和导出。 | “UI 当前显示版本”或“模型保证正确的原始音频复刻”。 |
| **字幕历史（Transcript History）** | 已正常结束或被中断会话的持久化文本视图，默认展示权威原始转写，并可在存在时明确切换到独立精修稿；活动会话进入终态后才出现在历史列表。 | “录音回放”或只保留精修后的单一文本——产品不保存原始音频，也不允许派生版本遮蔽原始转写。 |
| **摘要（Summary）** | 根据一个会话在明确输入水位和明确文本版本上生成的内容派生产物。首版是会后结构化纪要，摘要不能反向修改任何原始转写或精修稿。 | “最终结论”或“精修稿”——摘要可能随输入版本、水位和模型变化更新。 |
| **会后结构化纪要（Meeting Minutes）** | 会话停止后，基于该会话完整提交水位生成的结构化摘要，首版固定栏目为概要、结论、待办、风险；待办只生成文字。 | “自动执行待办”“会中滚动摘要”。 |
| **输入水位（Input Watermark）** | 摘要或其他派生产物已经消费到的事件边界，用于证明覆盖范围、去重和恢复。 | “最新”——必须能落到可检查的事件边界。 |
| **语义索引（Semantic Index）** | 后期可选、可重建且绑定明确文本版本的向量表示，用于相似度检索；默认索引权威原始转写，不属于字幕 MVP或任何文本版本的事实来源。 | “向量数据库”——本项目不引入独立远端权威数据源。 |
| **用户旅程（User Journey）** | 从用户意图开始，跨越多个真实产品模块并得到可观察结果的一条完整路径。 | “接口调通”——单个接口成功不等于旅程成立。 |
| **窗口交互代次（Window Interaction Generation）** | 应用主进程为窗口拖动、拉伸与鼠标穿透维护的单调递增内存标识。每次应用最小化事务推进一次；每次任务栏恢复或第二实例 `restoreOrShow` 事务再推进一次，并在该次恢复事务内让 `suspend → resume` 使用同一新代次。推进后，此前代次的 renderer 意图、待执行命中回调和手势状态全部失效；恢复窗口几何与层级后，主进程按当前指针位置要求对应 renderer 重新执行既有命中判定。它独立于工具条布局代次，不持久化，也不进入日志或证据报告。 | “恢复原手势”——最小化会取消当前手势，恢复后必须重新按下；也不要把窗口交互代次简称为布局代次或状态版本。 |
| **确定性联合测试（Deterministic Integration Journey）** | 在 CI 中运行的用户旅程测试，只在声卡、网络、系统权限等不可确定外部边界使用替身，内部产品模块使用真实实现。 | “大单测”“全 mock 集成测试”。 |
| **冻结字幕可见延迟（Frozen Caption Visibility Latency）** | 受控语料的播放源起点加已冻结语音起点偏移，到同一个已被 `SessionCoordinator` 接受并交给界面观察者的首个 partial 之间的时长。I2 每个来源固定 5 轮，以 P95 `<1000 ms` 独立验收。 | “模型推理耗时”“捕获到首字”——它覆盖产品链路且起点不随诊断算法变化。 |
| **跨时钟域延迟分段证据（Cross-clock-domain Latency Trace）** | 先用最小往返样本完成 playback renderer、audio-host renderer、utility process 与 main process 的 NTP 式单调时钟校准，再以同一未来 `source t0` arm 诊断并 schedule 播放；把首个已接受 partial 链路拆成可望远镜求和的诊断区段。报告只保留相对时长、帧序号、固定规则参数及校准质量摘要。 | 发布门槛；也不得持久化时钟偏移、绝对单调时刻、设备名、正文或音频来换取可观测性。 |
| **捕获能量起点（Captured Energy Onset）** | audio host 从固定的 `source t0 + 40ms` post-source guard 后，在收到的 PCM 上首次越过两段连续 20ms、-45dBFS 阈值的诊断观测；用于辅助区分采集等待与后续处理。40ms guard 只隔离前置环境声/跨时钟不确定区，冻结验收起点仍为 `source t0 + 140ms`。它不证明该能量来自受控语料或某位说话人。 | “真实发音起点”或“新的验收起点”——mic 可在 guard 后立刻被环境声触发，负的 `capturedOnsetMinusFrozenEstimateMs` 不能降低冻结延迟。 |
| **打包态确定性资格（Packaged Deterministic Qualification）** | 同一提交生成 Windows x64 ASAR/NSIS，并在受控外部边界下验证 package allowlist、native unpack/实际加载、utility process、SQLite、四窗产品旅程和安装/卸载机械生命周期。 | “干净机发布验收”——它不证明公网、真实权限、物理音源、交互安装向导、SmartScreen 或两小时稳定性。 |
| **干净机发布验收（Clean-machine Release Acceptance）** | 用 B5 精确安装器及其 SHA，在无仓库、无 Node、无既有 userData/模型的 Win11 机器上完成交互安装、公网资源供给、真实音源、持久化、离线复启与卸载策略验证。 | GitHub hosted runner、`win-unpacked` 启动或受控 fixture 旅程。 |
| **完整模型供给后离线（Offline After Complete Model Provisioning）** | 新安装首次取得核心字幕模型资源包需要网络；临时字幕识别器、权威识别器与 VAD 的严格 ready marker 已成立后，断网且 Agent 不存在时，字幕、SQLite 保存与历史查看仍必须工作。精修模型是否另行安装或启用不改变这条核心离线能力。 | “全新缺模型安装可离线首次使用”；把精修模型算入核心离线前置条件。 |
| **程序卸载（Application Uninstall）** | 移除安装目录、快捷方式和卸载登记的程序生命周期动作。MVP 默认不等于清除用户数据。 | “恢复出厂”“删除历史与模型”。 |
| **用户数据清除（User Data Erasure）** | 由用户明确触发、只针对精确 userData 根的模型、配置和字幕历史删除动作；当前 MVP 不提供。 | NSIS 默认卸载；不得把保留数据称为卸载失败。 |
| **实机证据（Machine Evidence）** | 在具备真实 Windows 音频、模型、窗口或长稳环境的机器上生成的可追溯 smoke/soak 报告。 | “CI 已通过”——托管 CI 不能替代硬件证据。 |
| **退出绑定运行证据（Exit-bound Run Evidence）** | 同一轮产品路径结果与外部观察到的同一受测应用实例正常退出结果一一绑定后，该轮才可计入 I2 权威序列；内部结果为 `pass` 后悬挂、超时或被运行器终止的轮次不合格。 | “产品路径报告为 `pass` 就等于自然退出”；它也不表示证据已签名、来自远端、证明了物理硬件或定位了崩溃根因。 |
| **实现完成（Implementation Complete）** | 代码与局部回归已完成，但联合旅程或实机门禁仍可能未通过。 | 无限定词的“完成”。 |
| **验收完成（Acceptance Complete）** | 该能力要求的确定性联合旅程和适用的实机证据均已通过。 | “单测全绿”。 |
| **降级（Degraded Capability）** | 某项可选能力不可用，但系统继续提供不依赖它的核心能力并在约定边界明确暴露事实。精修 worker 故障时，实时字幕立即回到首次稳定转写显示；停止后工具条用会话状态通知明确报告故障与覆盖并提供“查看历史”，详细结果持久化到历史，同时写入不含字幕正文、现场音频、本地路径或原始 Error 的结构化诊断日志。 | 永久静默失败；把不完整结果假装成完整成功；只靠颜色表达故障；为记录故障而保存正文、现场音频或敏感路径。 |
