# 功能与验收语义合同

> 状态：Accepted · 2026-08-01
>
> 目的：冻结“项目承诺了什么、没有承诺什么、需要什么证据才能称为完成”。
>
> 2026-08-01 的 [ADR 0004](adr/0004-immutable-first-pass-and-optional-refinement.md) 已接受“首次 `final` 不可变、精修默认关闭且独立保存”的新边界；历史 `final→refined` 证据仍是旧候选的事实记录，但不能证明新边界已经实现。

## 1. 文档职责与冲突处理

| 文档 | 唯一职责 |
|---|---|
| [`CONTEXT.md`](../CONTEXT.md) | 规范术语及其精确定义 |
| **本文** | 功能要求、测试边界与完成口径 |
| [`adr/`](adr/) | 已接受架构决策及其替代关系 |
| [`data-architecture.md`](data-architecture.md) | SQLite 字幕事实、Agent 派生产物与后置索引的实现约束 |
| [`testing-strategy.md`](testing-strategy.md) | 测试层级、旅程 ID、执行环境和当前证据 |
| [`../PLAN.md`](../PLAN.md) | 排期、阶段和实时状态，不重新定义语义 |

如果代码或状态文档与本文冲突，先把差异记录为实现缺口；不能悄悄用现状改写要求。若要改变本文，必须同时说明受影响的旅程、验收门禁和迁移方式；涉及难以回退的架构选择时新增或 supersede ADR。

## 2. 规范语义表

下表中的“必须”是验收要求。“当前状态”只陈述截至 2026-08-01 的证据，不能用来降低要求。

| ID | 主题 | 冻结语义与必须行为 | 明确不表示 / 禁止理解 | 必需证据 | 当前状态 |
|---|---|---|---|---|---|
| **SEM-F00** | 双系统边界 | 产品由可独立交付的字幕系统与可选 Agent 系统协作组成。字幕系统负责采集、ASR、显示、持久化和历史；Agent 系统只能从字幕提交边界消费已落盘正文并生成派生产物。 | Agent 不得成为开始监听、显示字幕、保存历史或查看原文的前置依赖；两套系统不表示两个安装包；“插件化”不得反转依赖。 | 字幕 MVP 的 J1/J2/J10 在 AI 完全关闭时通过；Agent 故障走 J7。 | 边界已决定；现有运行时尚未实现 Agent 系统。 |
| **SEM-F01** | 系统音频监听 | “监听系统音频”指 Windows `loopback` 来源进入 VAD/ASR，产生带 `sourceId=loopback` 的字幕并进入同一会话后续能力。 | 不叫“本地声音”；不保证识别出具体远端说话人；确定性 CI 不证明真实声卡。 | J1 + I2 loopback smoke；涉及摘要时再加 J3。 | 2026-07-31 schema-v6 退出绑定 series 已连续 5 轮验证 loopback→真实 ASR→精修：每轮 final/refined、最大双 CER 0、captured/sent/ingested 一致且 12 项丢失峰值全为 0，并分别绑定 exact child `exitCode=0`/无运行器终止的外部旁证；冻结字幕可见延迟 P50/P95/min/max 为 1133/1158/1092/1158ms。P95 高于冻结 `<1000ms` 线 158ms，I2 性能债未关闭。每轮另有 exact accepted-partial 的六段跨时钟诊断，但它不改变验收起点或门槛。J1 确定性链路已覆盖。 |
| **SEM-F02** | 互斥监听模式与来源归因 | 每个会话必须且只能选择 `loopback` 或 `mic`。`loopback` 是会议字幕主模式，`mic` 保留为单路个人听写；活动会话中不得并发两路或直接换源，停止后才能以另一来源开始新会话。 | 不提供 `mic + loopback` 双路产品模式；`mic=我`、`loopback=对方` 也不是身份承诺；当前不包含 diarization。 | J1/J2/J4；两种来源分别做 I2 实机 smoke，不再要求双路 soak。 | 产品各层和 J4 均执行 XOR；I2 runner 已将 loopback 与 mic 分进程运行。mic 证据是同轮 Gate 0C 匿名输入/输出标签绑定的 `physical-preferred-label-heuristic` 声学 fixture：可防预检后静默换标签，但不是硬件证明，也不能排除未知或伪造标签的虚拟设备；它不改变产品默认输入，也不支持双路并发。 |
| **SEM-F03** | 临时字幕 | `partial` 是当前字幕段不断变化的完整临时假设，只用于低延迟 UI；不能写入权威会话、摘要、FTS 或向量索引，也不能在 reload 后伪装成已定稿历史。视觉换行、行淘汰或窗口宽度都不能把它提前变成 `final`。 | `partial` 不是只含新增字符的增量，也不是可恢复原文；界面出现文字不等于会话已持久化。 | reducer/存储局部测试 + J1/J2/J15a。 | “partial 不落盘”已有覆盖；固定高度换行与识别边界隔离尚未实现。 |
| **SEM-F04** | 首次稳定转写与可选精修 | 每段第一次 `final` 是不可变的权威原始转写。精修默认关闭、按需供给并按会话冻结；启用后只能生成独立精修稿，原始版和精修版必须分别保存、查看和导出，默认始终选择原始版。 | `final` 不表示客观正确，但不得被 `refined` 覆盖；精修稿不是更高权威字幕事实，也不是摘要；用户选择显示精修稿不改变原始版本。 | 版本隔离、默认选择 + J1/J2/J5/J6/J15b；按会话开关与精修故障降级 + J15c。 | 语义已由 ADR 0004 接受，尚未实现；当前代码仍让 `refined` 替换 reducer/SQLite 投影/历史与导出，因此旧 final→refined 证据不能关闭本条。 |
| **SEM-F05** | 翻译 | 翻译属于 Agent 系统派生能力，必须绑定 `segmentId` 和 `basedOnRevision`；过期翻译不得覆盖新正文对应的译文，翻译失败不影响原文。B3.1 遗留 JSONL 中的 `translated` 记录只读保留，B3.3 不把它导入字幕 `caption_events`，未来由 Agent 迁移进入独立派生表。 | 翻译不是字幕 MVP，也不是正文修订；测试中注入 translated 事件不等于真实 AI 翻译已实现；不得为了旧格式逐字节一致而把译文混入新的字幕事实或原文导出。 | 契约/折叠测试；B3.3 的 DB2/J10 校验原文投影；真实 Agent 能力加入 J5/J7。 | 迁移内核和默认产品冷启动联合 CI 已确认遗留 translated 只计数且旧 JSONL 不改写，不进入 SQLite 字幕事实、当前原文或 txt/md/srt digest；Agent 生成与派生表迁移未实现。 |
| **SEM-F06** | 会话隔离 | 字幕、翻译、增强文本、摘要和可选索引结果都必须携带并校验 `sessionId`；新会话不得消费上一会话的迟到结果。 | renderer reload、pause/resume 或 worker replacement 不创建新会话。 | 状态机测试 + J5/J6；跨会话故障注入。 | 字幕核心状态已有回归；Agent 派生物尚未覆盖。 |
| **SEM-F07** | 权威持久化 | 字幕系统的目标唯一权威存储是 SQLite：不可变字幕事件是事实，segments 是可重建当前投影。B3.3 验收前，现有 JSONL 只是已实现过渡基线；迁移后只作导入导出/恢复。 | 不允许 SQLite/JSONL 长期双写；Agent 产物、向量、UI 状态或单个可变 transcript 行都不是字幕事实来源。 | DB0–DB2 + J1/J2 在 SQLite 后端重跑 + J10。 | 默认 `main.js` 已一次性切换到 `StorageGateway → SqliteSessionRecorder`，生产 JSONL writer 已删除。确定性产品/历史与真实 ASAR 旅程已验证旧档只读首启迁移、SQLite-only 新会话、同 userData 离线复启幂等、终态列表/205 段详情/三格式导出和两次退出屏障；ASAR 内 DB0 的 17 项 WAL/事务/重开/integrity 资格也已通过。I3 非音频 3,600 段重开/资源预资格通过；真实两小时声源与干净机 I4 仍待。 |
| **SEM-F08** | 字幕与会后结构化纪要 | 摘要属于 Agent 系统。首版在会话停止后默认消费越过字幕提交边界的权威原始转写；若用户明确选择精修稿，产物必须记录所用版本、完整输入水位和 digest，再生成概要、结论、待办、风险。 | 不消费 partial；摘要或提炼不得回写原始转写/精修稿；首版不承诺会中滚动摘要；待办只是内容，不自动执行。 | Agent 阶段的 J3/J5/J6/J7/J13 全部阻断；长稳另验。 | 语义已决定，尚未实现；不阻断字幕 MVP。 |
| **SEM-F09** | AI 故障降级 | AI 关闭、超时、限流、断网、凭据失效或 worker 崩溃时，本地采集、字幕、精修、持久化和导出继续；错误只降低 AI capability。 | 不允许把 AI 故障升级为会话停止；不允许静默丢任务后仍显示“已完成”。 | J6/J7 + 重启重放测试。 | 规则已冻结，AI 层未实现。 |
| **SEM-F10** | 可选语义检索 | `sqlite-vec` 是后期附加能力，只能索引声明了版本的文本，默认使用权威原始转写；结果必须匹配版本、输入 digest、模型和维度。扩展不可用时字幕历史必须完整可用。 | 向量不属于字幕 MVP，不是 SQLite 选型或 Agent 摘要上线的前置；删除索引不能删除原始转写或精修稿。 | 后期启用时执行 DB4 + J11 + 打包版门禁。 | 已后置，近期略去实现。 |
| **SEM-F11** | 带时间戳的字幕历史 | 字幕 MVP 必须自动保存首次稳定转写；终态会话按时间线默认展示和导出原始版本，存在精修稿时允许明确切换并随时返回原始版。renderer 仍按 keyset 读取有界页，完整导出不得受 UI 页大小影响。 | “回放”不表示音频播放；精修稿、摘要或当前 UI 选择不能遮蔽、删除或改写原始版本；把完整 transcript 暴露给 renderer 也不合格。 | J1/J2/J10/J15b；确定性长详情前置 + 两小时 J8。 | 现有列表、分页和三格式导出已实现，但读取的是 final/refined 单投影；原始默认、双版本查看与导出尚未实现，相关历史门禁需重新打开。 |
| **SEM-F12** | 暂停、恢复与崩溃 | pause/resume 保留同一会话；迟到结果按 revision/watermark 处理。ASR 或 storage worker 重启后，已提交字幕事实可恢复；Agent 待办后续必须可幂等重放。运行 worker 故障后即使用户不点重试/停止，也必须主动释放采集资源。native worker 正常收束先给 30 秒 graceful window；超时后只能终止并等待该 exact child，reap window 为 5 秒。字幕应用运行时以 45 秒作为优雅收束结束/升级触发线，ModelManager 的 5 秒收束与其并行；升级后仍须等待 exact child 收殓，因此 45 秒不是硬退出上限。一次性实机 runner 最多等待 exact Electron 120 秒；超时必须判失败，且清理只能作用于它启动的 process object。 | 不允许为了恢复接受跨会话事件；不允许在 error 状态继续占用 mic/loopback；旧 native worker 未确认退出前不得启动 replacement generation；不得按进程名批量终止 Electron；不得用“硬总上限”绕过 exact-child exit 确认；Agent 恢复不得反向阻塞字幕恢复。诊断通过、退出旁证通过或未再次复现都不等于根因定位、物理设备/睡眠、I3 或 I4 验收。 | J5/J6 + DB1 + I2 故障 smoke；真实模型活跃生命周期诊断；受监督产品壳 role evidence；exact-process timeout 与退出绑定回归。 | realtime/refine/storage UtilityProcess 已接 fatal `error`、角色标签和 exact-child exit 屏障；Coordinator 在旧世代 retirement 完成前禁止 replacement generation。2026-08-01 真实 loopback pause/refine 与 exact realtime worker 强制终止+Retry 均通过；后者证明同一 session/cursor、复用 adapter、新 worker generation、故障前后 final/refined 与零 transport 损失。另一次未纳入权威 bundle 的 `PostQueuedCompletionStatus(6)` 仍无 native stack，故历史异常根因未定位。性能、原生拖动、实际设备变化、睡眠/唤醒、I3/I4 与 Agent 仍待。 |
| **SEM-F13** | 独立增强文本 | LLM 修饰结果必须作为 `enhanced_transcript` 独立保存，声明 `sessionId`、输入水位、input digest、provider/model；权威转写始终可单独查看和导出。A2 首版只做会后或用户主动触发的整场增强。 | 增强文本不是 refined，不得覆盖、删除或伪装成 ASR 正文；失败不得改变字幕；按 committed segment 滚动增强不进入首版。 | J3/J5/J7 + artifact schema/版本测试。 | 保存和首版触发语义已决定，尚未实现。 |
| **SEM-F14** | 永不保存原始音频 | 原始 PCM、录音片段或可回放音频文件只允许在有界内存缓冲中服务实时 ASR/精修，使用后释放；产品、诊断和 smoke 现在及未来都不持久化现场采集音频。模型归档即使附带上游示例音频，产品安装也只能提取 manifest 白名单运行文件。 | 不允许在 SQLite BLOB、应用数据目录、日志、导出、崩溃恢复文件、测试产物、模型目录或 Agent 上下文中保存现场音频；不预留“以后打开录音”的隐藏路径。测试语料的 generator/reference 可以受跟踪，但生成 WAV 必须忽略且不得提交。跨时钟诊断也不得保存绝对单调时刻、校准偏移、设备名或正文。 | J12 + 存储/schema/诊断接口审查 + J14 安装目录检查 + I4 打包版数据目录检查。 | diagnostic、schema/RPC、Gateway、默认产品生命周期、历史复盘/三格式导出、模型 fixture 联合旅程与批准大模型真实安装均验证无现场音频产物、字段或路径且无 JSONL 双写；正式 ASAR 与整个 win-unpacked 目录负扫描也确认没有模型张量或音频载荷。schema-v5 I2 child report 只含设备/语料哈希、区段时长、校准质量摘要、指标与 privacy flags；schema-v1 exit sidecar 及 schema-v6 series 只增加退出绑定所需的固定状态与摘要。整个 bundle 不含正文、PCM、音频文件、路径、绝对时钟或偏移，并绑定本地生成 WAV 与受跟踪 reference 的摘要。真实安装曾发现并修复上游示例 WAV 被整包解出的缺口；I4 打包态真实 userData 检查和 DB6 总门禁仍待。 |
| **SEM-F15** | 内容型 Agent 边界 | Agent 首版只允许读取已提交文本、调用受控 ModelGateway、生成内容并写入内部 `agent_artifacts`。 | 禁止 shell、进程启动、任意文件写、任意网络请求和外部服务写操作；会议待办不能自动执行。 | J7/J13 + 插件能力清单审计。 | 语义已决定，Agent 尚未实现。 |
| **SEM-F16** | 可插拔 Agent 能力 | Pi 低层 Agent 核心外必须有项目自有插件宿主；字幕通过只读字幕上下文插件接入，增强文本和会后纪要分别作为内容产物插件。字幕系统本身仍独立运行。 | 不直接嵌入完整 coding-agent 扩展运行时；不让 Pi 插件拥有音频采集、ASR、字幕会话或字幕数据库写权。 | A1 探针 + J3/J7/J13；插件卸载/失败时字幕 J1 仍成立。 | 宿主边界已由 ADR 0003 接受；Pi/PluginHost 尚未实现或安装。 |
| **SEM-F17** | 本地模型资源闭环 | 核心字幕 ready 只依赖受审计、可恢复安装的实时 ASR 与 VAD。精修模型是默认不下载的独立可选资源，只能由未监听状态下的明确用户动作按需安装；缺失、下载失败或运行失败不得阻止核心字幕、首次 `final`、历史和导出。 | 不得继续用“三项资源全部存在”阻塞默认字幕；活动会话中不得安装或切换精修策略；renderer 仍不能提供 URL、哈希、路径或解压参数。 | 核心与可选资源分别覆盖 J14/J15c + B5/I4；真实模型调用沿用/扩展 I2。 | 当前 ModelManager、ready marker 和 UI 仍把实时 ASR、离线精修、VAD 当作原子三项 bundle；旧安装证据有效描述旧候选，但新核心/可选拆分尚未实现或验收。 |
| **SEM-F18** | 打包字幕 MVP | B5 的目标是 Windows x64 打包态确定性资格：正式包使用 ASAR、单击式当前用户 NSIS 和 `asInvoker`；只打入产品源码与生产依赖，模型不入包；整个 sherpa 平台目录解包以保持 `.node`/DLL 相邻；打包态禁用开发缝并开启 ASAR/fuse 约束。测试 package variant 必须与正式布局分离并声明不是 release candidate。 | `win-unpacked` 可启动、文件存在或测试 variant 通过都不等于精确 NSIS 的干净机 I4；native 文件在磁盘不等于 utility 实际加载；unsigned 可安装不等于可公开可信分发。 | B5 package layout + packaged first/restart journeys + 两份 exact-child exit + 同轮/产品载荷 binding + NSIS lifecycle；I4 另验。 | 当前 B5 候选 SHA=`36c75120…95f4ae`：165 个 ASAR 条目、111 文件产品载荷 SHA=`6c986613…f577`、29 个必需入口、5 个 unpacked native、整包负扫描、packaged 首启/离线复启及隔离 NSIS 安装卸载均有结构化证据。候选未签名，按内部 MVP 暂缓；远端 workflow 与干净机 I4 尚未取得结果。 |
| **SEM-F19** | 程序卸载与用户数据 | MVP 程序卸载只移除程序、快捷方式和卸载登记，默认保留 `userData` 中模型、配置和 SQLite 历史；任何“删除本地数据”必须是以后单独、明确的用户动作，并限制在精确 userData 根。 | 不得让 NSIS 默认卸载静默删除字幕历史；“程序卸载成功”不等于用户数据已删除；保留数据也不等于卸载失败。 | B5 隔离 NSIS 机械生命周期 + I4 已产生 userData 后的卸载/重装验证。 | builder 固定 `deleteAppDataOnUninstall=false`；B5 只证明隔离 APPDATA 中与应用无关的哨兵不变。I4 非音频 runner 已改用 Known Folder 动态发现正式 userData，并比较 config/SQLite/legacy/三 marker 的卸载前后清单及离线重装复用；尚无专用干净机报告，因此真实数据保留仍未验收。 |
| **SEM-F20** | 固定高度字幕行流 | 字幕窗口不得因内容自动改变 bounds。文字按当前宽度自然换行；可见高度满后最旧完整视觉行退出、最新行保持在底部，跨段逐行淘汰，停顿时保留。`partial` 回改可重排，但布局永远不能修改识别文本或触发分段；可选精修只更新仍可见旧段，已淘汰段不复活，当前 `partial` 优先。 | 不使用横向跑马、自动增高、整窗清空、可见滚动条或按视觉宽度强制 VAD/final；右上角状态标识变色不在本次范围。 | DOM/reducer 固定 bounds 旅程，覆盖多字号、中英混排、长词、回改、跨段与停顿 + J15a；精修迟到只更新仍可见旧段的部分与 J15b 共同验收。 | 设计已接受，尚未实现；当前 `line-clamp` 与槽位行预算会隐藏长 `partial` 的后续内容。 |
| **SEM-T01** | 单元测试边界 | 单元测试只证明局部不变量，不能单独证明用户能力完成。 | “测试数很多”“覆盖率高”都不能替代用户旅程。 | 至少一条登记的确定性联合旅程。 | 规则已生效。 |
| **SEM-T02** | 联合测试边界 | 联合测试必须让多个真实产品模块围绕同一用户结果协作；只在声卡、网络、系统权限、云 provider 等不可确定外部边界使用契约替身。 | 全链路 mock、直接调用最终 exporter、只断言某函数被调用都不算联合旅程。 | CI 测试列出真实模块、替身边界和用户可观察结果。 | J1/J2 已按此执行。 |
| **SEM-T03** | 托管 CI 边界 | Windows hosted CI 证明确定性接线、回归、故障注入和打包机械资格，不声称证明真实 WASAPI、物理麦克风、DWM、模型性能、SmartScreen、交互权限/安装向导、两小时稳定性或“干净机”。 | CI 绿色、静默 NSIS 安装成功或 `win-unpacked` 运行成功都不等于实机/发布验收完成。 | 对应 I2/I3/I4 结构化实机报告。 | 规则已生效；I2 有两来源 exit-bound bundle，B5 有当前候选证据，I3 有明确 `pass/partial` 的 3,600 段非音频预资格。它们均不冒充真实两小时声源、签名、远端或干净机证明。 |
| **SEM-T04** | 失败路径 | 每项用户能力至少覆盖一个关键失败或降级路径；核心能力必须 fail closed 或显式降级。 | 只覆盖 happy path 不得标记验收完成。 | 场景矩阵中的故障旅程或实机故障注入。 | SQLite 事实、Gateway 队列、DB2 第二文件中断、产品启动迁移失败清理和退出超时强制终止已有回归；历史层覆盖活动会话拒绝、越权载荷拒绝和取消导出不写文件；模型层覆盖坏 hash/size、越权 host/path、归档 traversal/link、活动会话拒绝替换，以及 fetch/tar 无视 abort 时的有界退出、合法 `.part` 保留与下次启动清 staging。Agent 失败路径待实现，向量已后置。 |
| **SEM-T05** | 完成状态词 | 只允许“已决定”“实现完成/尚未验收”“联合验收完成”“实机验收完成”“发布验收完成”。无修饰的“完成”必须能指向所需最高门禁。 | 不允许用“已接通”“可用”“测试通过”替代明确状态。 | PLAN/README 状态与测试报告链接一致。 | 从本次文档起执行。 |
| **SEM-T06** | 场景登记 | 新功能或语义变化必须先在本表和 `testing-strategy.md` 增加/更新旅程，再实现；修复不能只新增孤立单测。 | 未登记的隐藏验收口径、开发完成后再补需求均不接受。 | PR 同时引用 SEM ID 与 J/I/DB gate。 | 规则已生效。 |
| **SEM-T07** | Agent 纪要完成门禁 | 字幕→会后纪要必须分别从 `loopback` 与 `mic` 单路会话验证，并覆盖 refine、暂停恢复/崩溃、AI 失败和来源互斥；不测试或支持双路并发。 | 只用一段固定文本调用 LLM 的测试不能称纪要功能完成；纪要未完成不影响字幕 MVP 验收。 | J3–J7/J13 + 适用实机证据。 | 未实现；Agent 阶段阻断。 |
| **SEM-T08** | 数据迁移门禁 | JSONL→SQLite 必须可中断、可重跑、可核对，并保留每段最早有效 `final` 作为权威原始转写；遗留 `refined` 必须保持可审计且迁移为可区分的精修版本，不能折叠后遮蔽原始版。原始版与精修版的详情和导出 digest 分别核对。 | 只核对旧“final/refined 当前投影”、只保留最高 revision、或让旧精修稿成为默认原文，都不能满足新迁移边界；仍禁止长期双写。 | DB2 + J10 + J15b。 | 旧 DB2/J10 已证明单投影迁移的原子、幂等和完整导出，但没有证明首次 `final` 默认可见或双版本隔离；该门禁因 ADR 0004 重新打开。 |
| **SEM-T09** | 向量门禁（后置） | 只有未来启用 `sqlite-vec` 时，才必须通过开发版/打包版加载、旧向量失效、索引重建、扩展缺失降级和不同模型维度隔离。 | 本门禁不得阻断 SQLite 历史、字幕 MVP 或首版 Agent 摘要。 | DB4 + J11 + 对应打包版验收。 | Deferred；近期不执行。 |
| **SEM-T10** | 插件联合门禁 | 每个 Agent 插件必须在真实插件宿主、字幕提交边界、SQLite 产物存储和 UI 读取链路中测试；同时覆盖插件超时/异常/重复触发/卸载及越权请求。 | 直接调用插件函数、mock 掉宿主和数据库或只验证 prompt 文本，不等于插件能力完成。 | J3/J7/J13；A1/A2 PR 阻断。 | 规则已冻结，宿主尚未实现。 |
| **SEM-T11** | 模型供应链门禁 | 确定性 CI 必须分别证明核心实时 ASR+VAD 的首次供给/离线复启，以及用户按需安装精修模型的独立流程；两条流程都保留固定 manifest、续传、字节/SHA、归档白名单、staging 与原子 ready marker，且活动会话拒绝替换。 | 三项一次性 bundle 通过不能证明默认不下载精修、核心独立 ready、按会话开关或可选模型故障降级。 | J14/J15c；真实模型调用 I2/I4。 | 旧三项 bundle 的确定性与真实调用证据仍是历史事实；核心/可选拆分及其打包、断网和失败门禁尚未实现。 |
| **SEM-T12** | 打包与发布证据分层 | B5 打包态确定性资格与 I4 干净机发布验收是两个独立门禁。B5 必须绑定精确制品 SHA，并分别证明正式 package layout、受控 packaged 首启/复启、native/SQLite utility、两次 exact-child 退出、同轮 run ID/四份报告 SHA/完整产品载荷 SHA 和 NSIS 生命周期；I4 必须用同一精确安装器在无仓库/Node/既有数据的 Win11 上执行公网、权限、真实音源、离线复启和数据保留。I4 可拆出无 capture 的 `pass/partial` 子报告，但它不能关闭完整门禁。 | 任何单份 B5 或 I4 非音频报告都不得把受控资源/fake ASR/静默安装/断网成功冒充公网流量审计、真实模型推理、物理来源或完整发布验收；clean exit 不等于 `0x80000003` 已根因定位。 | B5 七份结构化报告 + I4 非音频 partial report + I4 音频 release acceptance report。 | 当前 B5 七份证据已绑定 SHA `36c75120…95f4ae` 候选；I4 非音频 runner/verifier 已完成但无专用机报告，音频报告也未执行，故仍不能写“发布验收完成”。 |
| **SEM-T13** | 产品路径 I2 证据 | `loopback` 与 `mic` 必须分开执行真实 Electron/audio-host/online ASR/VAD/offline refine；当前权威 series 每来源固定 5 轮形成 P50/P95/min/max。每轮 schema-v5 child report 只有与外部运行器在 exact child `exitCode=0` 且未触发运行器终止后生成的 schema-v1 exit sidecar 按 report SHA 一一绑定，才可进入 schema-v6 series；缺失、多余、重复、错序、错绑或未知字段必须 fail closed。自动 mic fixture 必须先通过同轮 memory-only Gate 0C，绑定精确 preflight SHA、匿名输入/输出 label SHA，并以唯一标签匹配防止预检后静默换端点；其规范名是 `physical-preferred-label-heuristic` 声学 fixture。整个 bundle 只能写指标、哈希和固定语义，不写正文、PCM、音频文件或路径；原始证据 JSON 必须在对象校验前拒绝非法 UTF-8、BOM、重复键、非有限数值与尾随输入，再执行闭合字段验证；语料 WAV 本地生成且忽略，报告绑定其 digest 与受跟踪 reference digest。 | label heuristic 不是硬件证明，不能排除未知或伪造标签的虚拟设备；exit sidecar 只防止内部 `pass` 后悬挂/超时或被运行器终止的误绿，不是签名、远端执行、硬件身份、崩溃根因证明；VB-Cable、系统默认输入、单次通过、模型裸 benchmark 或托管 CI 都不能冒充更强的 mic 结论；5 轮 happy-path 也不等于性能、拖动、设备、睡眠、I3 或 I4 完成。 | 权威 bundle `docs/validation/i2-live-v5/`：Gate 0C SHA-256 `0f9f7668751c64fbce922883421ead41680226126800e0b7f6b3da81b39840ef` + 两来源各 5 个 schema-v5 child、5 个 schema-v1 exit sidecar 与 1 份 schema-v6 series；其余 I2 实机交互场景另报。 | 受跟踪的 2026-07-31 两来源各 5/5，冻结 P95=1158/1005ms；2026-08-01 当前机复跑仍各 5/5 结构/准确率/自然退出/零损失，冻结 P95=1148/1099ms。两批均未满足 `<1000ms`。真实 pause/refine 与 exact worker 强制终止+Retry 已通过；原生拖动、实际设备移除、睡眠/唤醒仍待，因此 I2 整体未关闭。 |
| **SEM-T14** | 首字延迟观测边界 | 冻结字幕可见延迟是唯一 I2 首字验收值，必须从冻结语料 `source t0 + 140ms` 固定 onset 算到 `SessionCoordinator` 接受并通知观察者的同一个首个 partial。现场运行必须先完成 playback/audio-host/utility 的固定样本时钟校准，再预留同一个未来 `source t0`，先按该时刻 arm 捕获诊断、再按该时刻 schedule 播放，不能让校准或 IPC 偷吃 140ms onset 预算。schema-v5 child 可用 NTP 式最小 RTT 校准，把冻结值拆成六个非负整数区段；同一 child 的六段必须精确望远镜求和，校准必须有固定 clock ID、样本数、RTT/不确定度/年龄上限。captured energy onset 只作阈值诊断，并固定从 `source t0 + 40ms` 的 post-source guard 后观察；该 guard 仅排除前置环境声/亚毫秒跨时钟不确定区，仍比冻结 onset 早 100ms。 | 不得把 40ms guard、captured onset、VAD 事件、worker publish、DOM paint 或各区段单独替代冻结端到端门槛；不得把各轮区段 P95 相加冒充某轮总延迟；不得 clamp 因果倒置、移动 `source t0 + 140ms`、改变 onset 规则或用 guard 制造达标。mic 的负 `capturedOnsetMinusFrozenEstimateMs` 只表示 guard 后已有环境能量；它既不证明语料/说话人起音，也不能降低冻结延迟。 | 任意远端时钟原点/错误 ID/高 RTT/陈旧校准/因果倒置纯测试；校准先于 shared-t0 arm/schedule 的延迟 IPC 测试；audio-host→worker→adapter→Coordinator 的 exact accepted-partial 组合测试；schema-v5 两来源各 5 轮实机证据。 | 六段链路及严格 schema 已实现并通过纯测试、组合测试和 5+5 实机取证。series P95 依次为 loopback 729/405/1/33/1/1ms，mic 557/500/1/28/1/1ms；这些诊断显示主要预算位于采集/VAD 与触发所需音频，而不是 IPC。冻结 P95 仍为 loopback 1158ms、mic 1005ms，两者均未被 captured-energy 或分段诊断改判通过。 |

> ADR 0004 解释规则：上表历史状态中出现的 `final/refined`、三资源 bundle 或 offline refine 只描述旧候选证据。新的默认原始路径必须在不启用精修时独立通过；SEM-T13 的 offline refine 要求仅适用于用户明确启用的可选精修变体，不能重新成为核心字幕 ready 条件。

## 3. 变更维护规则

1. 需求、实现或测试 PR 必须引用至少一个 `SEM-*`；没有适用项时先新增语义行。
2. 改“冻结语义”时必须更新对应 J/DB/I gate；只改“当前状态”不改变要求。
3. 旅程通过后在测试策略和 PLAN 更新证据，但不得删除失败路径或缩小边界来制造通过。
4. “当前状态”至少在阶段验收、存储后端切换、摘要上线和发布前复核一次。
5. 模糊词按 [`CONTEXT.md`](../CONTEXT.md) 替换；特别禁止用“本地声音”“最终字幕”“单测通过所以完成”。
