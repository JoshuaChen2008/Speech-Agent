# 功能与验收语义合同

> 状态：已决定 · 2026-08-09
>
> 目的：冻结“项目承诺了什么、没有承诺什么、需要什么证据才能称为完成”。
>
> 2026-08-01 的 [ADR 0004](adr/0004-immutable-first-pass-and-optional-refinement.md) 已接受“首次 `final` 不可变、精修默认关闭且独立保存”的新边界；历史 `final→refined` 证据仍是旧候选的事实记录，但不能证明新边界已经实现。

## 1. 文档职责与冲突处理

| 文档 | 唯一职责 |
|---|---|
| [`CONTEXT.md`](../CONTEXT.md) | 规范术语及其精确定义 |
| **本文** | 功能要求、测试边界与完成口径 |
| [`adr/`](adr/) | 已接受架构决策及其替代关系 |
| [`data-architecture.md`](data-architecture.md) | SQLite 字幕事实、Agent 派生产物与后置索引的实现约束 |
| [`testing-strategy.md`](testing-strategy.md) | 测试层级、旅程 ID、执行环境和当前证据 |
| [`../PLAN.md`](../PLAN.md) | 排期、阶段和实时状态，不重新定义语义 |

如果代码或状态文档与本文冲突，先把差异记录为实现缺口；不能悄悄用现状改写要求。若要改变本文，必须同时说明受影响的旅程、验收门禁和迁移方式；涉及难以回退的架构选择时新增或 supersede ADR。

## 2. 规范语义表

下表中的“必须”是验收要求。“当前状态”只陈述截至 2026-08-09 的证据，不能用来降低要求。

> 2026-08-07 历史状态复核（已由下方当前确定性资格取代）：当时的 revision `a91a200cc2d3df5f73dcb7a0894a9758e532106a` 在正常 Windows 子进程权限下取得 core 414 tests=407 pass+7 expected skips、integration 27/27、evidence 200/200；总计 641 tests=634 pass+7 expected skips+0 fail。当时最近一次 J9-CI 联合验收完成基线为 run `30792514100` / revision `2890dd9a5a224bca75cff2257cfd14013d74584d`，631 tests=624 pass+7 expected skips+0 fail；artifact ID `8847650958`、GitHub ZIP digest `9e4b2d7b9975afc288c5a14728e0a8812ec00eea1f6a25f46080063c8beccafd`，下载侧严格复核与隐私负扫描闭合，索引绑定 installer SHA `25ff62a88efb8e8d021887df3a2685c72515baadca23f4810902f13d2ae19b76`。该历史 revision 的 run `30801570384` 已通过产品与打包前置，但 evidence 200 tests=197 pass+3 fail，最终 provenance 索引按设计跳过；`scripts/gate-0b/corpus.json` 未固定 LF，hosted CRLF SHA `fdf4420a243cc6e0efe0074dbf5c60c97efc1d1284448b7bfce9c3d658e1fce9` 与登记的 LF SHA `7edd6dff286b84619a3b68f385ba04622103ffe17cc57dbe5e1f16521deb156d` 不同，故该历史 revision 的 SEM-T03/J9-CI 为实现完成·尚未验收。当时 `src/`、`package.json`、锁文件和 builder 配置自 `2d3d6bdf5d745c15c239ef7503a0dfac211409a8` 起未变化，SEM-F18/SEM-T12 的本机 B5 产品载荷绑定不变。

| ID | 主题 | 冻结语义与必须行为 | 明确不表示 / 禁止理解 | 必需证据 | 当前状态 |
|---|---|---|---|---|---|
| **SEM-F00** | 双系统边界 | 产品由可独立交付的字幕系统与可选 Agent 系统协作组成。字幕系统负责采集、ASR、显示、持久化和历史；Agent 系统只能从字幕提交边界消费已落盘正文并生成派生产物。 | Agent 不得成为开始监听、显示字幕、保存历史或查看原文的前置依赖；两套系统不表示两个安装包；“插件化”不得反转依赖。 | 字幕 MVP 的 J1/J2/J10 在 Agent 系统完全关闭时通过；Agent 故障走 J7。 | 已决定：正式产品运行时仍未接入 Agent 系统；SEM-F29/J23 的隔离 Agent 内核开发入口已存在，但与字幕主进程、正式 userData 和正式安装包隔离，不改变本行依赖边界。 |
| **SEM-F01** | 系统音频监听 | “监听系统音频”指 Windows `loopback` 来源进入 VAD/ASR，产生带 `sourceId=loopback` 的字幕并进入同一会话后续能力。Silero 确认人声前允许用能量门控帧预热 recognizer，但四帧（4×100ms）段前缓冲与十二帧（12×100ms）provisional stream 上限必须独立：上限内确认人声时复用已预热 stream，达到上限仍未确认时立即丢弃并停止继续预热，后续新的候选必须能重新建立 stream。确认前不得产生字幕段、发出临时字幕或持久化任何正文；threshold `0.5`、minSpeechDuration `0.25`、minSilenceDuration `1.0` 与冻结验收起点 `source t0 + 140ms` 不因优化改变。 | 不叫“本地声音”；不保证识别出具体远端说话人；确定性 CI 不证明真实声卡。 | J1 + I2 loopback smoke；确定性负向矩阵覆盖冻结语料、997Hz 纯音、持续低能量噪声、确认前抑制及后续新语音恢复；涉及摘要时再加 J3。 | 2026-07-31 schema-v6 退出绑定 series 已连续 5 轮验证 loopback→真实 ASR→精修：每轮 final/refined、最大双 CER 0、captured/sent/ingested 一致且 12 项丢失峰值全为 0，并分别绑定 exact child `exitCode=0`/无运行器终止的外部旁证；冻结字幕可见延迟 P50/P95/min/max 为 1133/1158/1092/1158ms。P95 高于冻结 `<1000ms` 线 158ms，I2 性能债未关闭。每轮另有 exact accepted-partial 的六段跨时钟诊断，但它不改变验收起点或门槛。J1 确定性链路已覆盖。4/12 有界 provisional 预热解耦、确认前零字幕、上限抑制、后续新语音恢复、持续低能量噪声及真实冻结语料/997Hz 纯音回归为实现完成·尚未验收；revision `b96b8fe7db5ba4db3ac36c4ee85371a4381b521f` 的优化后 `loopback` 五轮已闭合结构、精修、自然退出与零损失，但冻结 P95=1242ms，仍不满足 `<1000ms`，因此不得提升 I2 状态。 |
| **SEM-F02** | 互斥监听模式与来源归因 | 每个会话必须且只能选择 `loopback` 或 `mic`。`loopback` 是会议字幕主模式，`mic` 保留为单路个人听写；活动会话中不得并发两路或直接换源，停止后才能以另一来源开始新会话。 | 不提供 `mic + loopback` 双路产品模式；`mic=我`、`loopback=对方` 也不是身份承诺；当前不包含 diarization。 | J1/J2/J4；两种来源分别做 I2 实机 smoke，不再要求双路 soak。 | 产品各层和 J4 均执行 XOR；I2 runner 已将 loopback 与 mic 分进程运行。mic 证据是同轮 Gate 0C 匿名输入/输出标签绑定的 `physical-preferred-label-heuristic` 声学 fixture：可防预检后静默换标签，但不是硬件证明，也不能排除未知或伪造标签的虚拟设备；它不改变产品默认输入，也不支持双路并发。 |
| **SEM-F03** | 临时字幕 | `partial` 是当前字幕段不断变化的完整临时假设，只用于低延迟 UI；不能写入权威会话、摘要、FTS 或向量索引，也不能在 reload 后伪装成已定稿历史。视觉换行、行淘汰或窗口宽度都不能把它提前变成 `final`。 | `partial` 不是只含新增字符的增量，也不是可恢复原文；界面出现文字不等于会话已持久化。 | reducer/存储局部测试 + J1/J2/J15a。 | “partial 不落盘”已有覆盖；固定高度换行与识别边界隔离已实现（J15a）：换行与行淘汰纯属渲染投影，布局资格另断言全程无窗口 resize 请求，也不产生 `final` 或持久化写入。 |
| **SEM-F04** | 首次稳定转写与可选精修 | 每段第一次 `final` 是不可变的权威原始转写。精修默认关闭、按需供给；一个不区分 `mic`/`loopback` 的全局精修偏好只决定未来新会话，会话开始时冻结，关闭不删除旧会话精修稿。启用后只能生成独立精修稿，原始版和精修版必须分别保存、查看和导出，默认始终选择原始版。精修稿到达时自动更新仍可见的已定稿字幕段，但不得修改当前 `partial` 或复活已淘汰段。精修 worker 中途失败一经确认，所有仍可见的已定稿段必须立即恢复各自首次稳定转写并在原固定视口内重新排版；当前 `partial` 原样保留且优先级不变，已淘汰段不复活，后续段继续显示首次稳定转写。会话继续、已有精修稿保留、本会话不重启 worker、不自动补跑。运行故障不得修改全局精修偏好；模型仍 ready 且偏好仍开启时，下一新会话可重新尝试启动一次，只有应用启动时模型就绪证明缺失或损坏才把偏好回落关闭。会话正常停止后，工具条必须在不抢焦点且不改变任何窗口 bounds 的前提下显示会话状态通知和“查看历史”；该通知只报告精修处理状态，不对字幕内容做摘要、提炼或改写，保持到用户关闭或进入历史，开始下一会话时自动清除，应用重启不重放。详细结果与会话一起持久化。`M` 包含全部已持久化首次稳定转写，包括停止收尾、队列限界或故障未精修段；`N` 是已有精修稿段数，`partial` 不计入。故障事实与覆盖度独立：`N < M` 不证明 worker 故障，`N = M` 也不得隐藏已确认故障。会话故障只允许 `REFINE_WORKER_START_FAILED`、`REFINE_WORKER_EXITED`、`REFINE_DECODE_FAILED`、`REFINE_INVALID_RESPONSE`、`REFINE_INTERNAL_FAILURE` 五个稳定错误码；启动时模型缺失或损坏不属于会话故障。 | `final` 不表示客观正确，但不得被 `refined` 覆盖；精修稿不是更高权威字幕事实，也不是摘要；用户选择显示精修稿不改变原始版本。不得删除已完成精修稿、因故障在同会话自动重跑、因运行故障擅自关闭未来会话精修、用当前页或排除跳过段来美化覆盖，或把全原始/不完整结果标成普通 `refined`。运行中不打扰回退不等于可以永久隐瞒故障；颜色不能成为唯一故障通道。 | 版本隔离、默认选择 + J1/J2/J5/J6/J15b；全局偏好、即时可见段恢复、独立持久化会话结果、工具条会话状态通知 + J15c。 | J15b/J15c 已达到联合验收完成：首次 `final` 锚定、独立精修稿、按会话版本选择、故障后可见原文恢复、工具条会话状态通知、会话结果持久化与结构化日志均有局部和跨模块证据；真实 packaged Electron 覆盖会话冻结、故障静默期、main→IPC→toolbar 通知、历史跳转与重启不重放。真实模型故障仍归 I2/I4。 |
| **SEM-F05** | 翻译 | 翻译属于 Agent 系统派生能力，必须绑定 `segmentId` 和 `basedOnRevision`；过期翻译不得覆盖新正文对应的译文，翻译失败不影响原文。B3.1 遗留 JSONL 中的 `translated` 记录只读保留，B3.3 不把它导入字幕 `caption_events`，未来由 Agent 迁移进入独立派生表。 | 翻译不是字幕 MVP，也不是正文修订；测试中注入 translated 事件不等于真实 Agent 翻译能力已实现；不得为了旧格式逐字节一致而把译文混入新的字幕事实或原文导出。 | 契约/折叠测试；B3.3 的 DB2/J10 校验原文投影；真实 Agent 能力加入 J5/J7。 | 迁移内核和默认产品冷启动联合 CI 已确认遗留 translated 只计数且旧 JSONL 不改写，不进入 SQLite 字幕事实、当前原文或 txt/md/srt digest；Agent 生成与派生表迁移未实现。 |
| **SEM-F06** | 会话隔离 | 字幕、翻译、增强文本、摘要和可选索引结果都必须携带并校验 `sessionId`；新会话不得消费上一会话的迟到结果。 | renderer reload、pause/resume 或 worker replacement 不创建新会话。 | 状态机测试 + J5/J6；跨会话故障注入。 | 字幕核心状态已有回归；Agent 派生物尚未覆盖。 |
| **SEM-F07** | 权威持久化 | 字幕系统的目标唯一权威存储是 SQLite：不可变字幕事件是事实，segments 是可重建当前投影。B3.3 验收前，现有 JSONL 只是已实现过渡基线；迁移后只作导入导出/恢复。 | 不允许 SQLite/JSONL 长期双写；Agent 产物、向量、UI 状态或单个可变 transcript 行都不是字幕事实来源。 | DB0–DB2 + J1/J2 在 SQLite 后端重跑 + J10。 | 默认 `main.js` 已一次性切换到 `StorageGateway → SqliteSessionRecorder`，生产 JSONL writer 已删除。确定性产品/历史与真实 ASAR 旅程已验证旧档只读首启迁移、SQLite-only 新会话、同 userData 离线复启幂等、终态列表/205 段详情/三格式导出和两次退出屏障；ASAR 内 DB0 的 17 项 WAL/事务/重开/integrity 资格也已通过。I3 非音频 3,600 段重开/资源预资格通过；真实两小时声源与干净机 I4 仍待。 |
| **SEM-F08** | 字幕与会后结构化纪要 | 摘要属于 Agent 系统。首版在会话停止后默认消费越过字幕提交边界的权威原始转写；若用户明确选择精修稿，产物必须记录所用版本、完整输入水位和 digest，再生成概要、结论、待办、风险。 | 不消费 partial；摘要或提炼不得回写原始转写/精修稿；首版不承诺会中滚动摘要；待办只是内容，不自动执行。 | Agent 阶段的 J3/J5/J6/J7/J13 全部阻断；长稳另验。 | 已决定；D4 的正式冻结输入读取、原始/完整精修稿、四栏目结构化产物与独立 SQLite 提交后端子边界为实现完成·尚未验收；尚无 `MeetingStopped`、历史 UI、双来源组合或实机证据，不阻断字幕 MVP。 |
| **SEM-F09** | Agent 故障降级 | Agent 关闭、超时、限流、断网、启动环境凭据缺失或失效、provider 配置不合法或 worker 崩溃时，本地采集、字幕、精修、持久化和导出继续；错误只降低 Agent 系统能力。 | 不允许把 Agent 故障升级为会话停止；不允许静默丢任务后仍显示“已完成”；不得因真实 Agent 模型 provider 尚未接入而伪造产物。 | J6/J7 + 重启重放测试。 | 已决定：SEM-F29/J23 隔离入口的故障矩阵已达到联合验收完成；D4/D5 正式 UI-free 后端的超时重试、取消、无效输出、插件超时/卸载、迟到提交拒绝与单项失败隔离，以及 D6 production `StorageWorkerHost` 在 claim 后结束 exact child、replacement 策略重放和同 `runId` 恢复的 storage utility transport 子边界，均为实现完成·尚未验收，且字幕事实保持不变。ADR 0011 已决定正式首版的 DeepSeek 非敏感配置表与 `DEEPSEEK_API_KEY` 启动环境凭据规则；正式产品接线、配置解析、限流/断网/凭据降级、Agent utility replacement 与完整 J7 仍无证据。 |
| **SEM-F10** | 可选语义检索 | `sqlite-vec` 是后期附加能力，只能索引声明了版本的文本，默认使用权威原始转写；结果必须匹配版本、输入 digest、模型和维度。扩展不可用时字幕历史必须完整可用。 | 向量不属于字幕 MVP，不是 SQLite 选型或 Agent 摘要上线的前置；删除索引不能删除原始转写或精修稿。 | 后期启用时执行 DB4 + J11 + 打包版门禁。 | 已后置，近期略去实现。 |
| **SEM-F11** | 带时间戳的字幕历史 | 字幕 MVP 必须自动保存首次稳定转写；终态会话按时间线默认展示和导出原始版本，至少存在一段精修稿时允许明确切换并随时返回原始版。关闭全局精修偏好只影响未来会话，旧会话精修稿仍可查看和导出。每次选择不同会话都必须重置为原始版；同一会话内翻页保留当前显示选择，导出跟随该会话当前明确选择。历史契约必须以有界元数据返回整场 `N/M` 覆盖和独立的精修会话故障事实；`M` 含所有已持久化首次稳定转写，renderer 不得从当前页估算或排除停止/队列/故障跳过段，也不得从 `N/M` 反推故障。`0 < N < M` 时显示“已精修 N/M 段，M−N 段使用原始版”，回退行统一标记 `[原始版回退]`；明确导出使用 `refined-incomplete` 文件名，txt/md 在文件头写覆盖提示并在行首加标记，srt 在 cue 正文前加标记。`M > 0` 且 `N = 0` 时显示“本会话未生成精修稿”并禁用精修视图/导出；`M = 0` 且无故障时不显示 `0/0` 或精修结果，若有故障则显示“精修进程异常结束；本会话未产生可精修的已定稿字幕”。若已确认 worker 故障但 `N = M`，仍显示“精修进程异常结束，但本次已生成 N/N 段精修稿”。旧版本会话和旧 JSONL 导入必须标为“未记录精修运行状态”，只在精修详情显示，普通历史列表不展示，也不得把无记录解释为无故障。原始版名称和正文始终不变。MVP 不要求按故障原因过滤或给每行展示技术原因；停止后工具条显示会话状态通知和“查看历史”，该通知只报告处理状态，不概括或改写字幕内容；详细结果在应用重启后仍从历史可见。若应用在正常停止前异常退出，重启后会话显示为中断，不重放旧通知，但历史继续显示已持久化故障及最终可计算覆盖。renderer 仍按 keyset 读取有界页，完整导出不得受 UI 页大小影响。 | “回放”不表示音频播放；精修稿、内容摘要、偏好关闭或上一个会话的显示选择不能遮蔽、删除或改写当前会话的原始/精修版本；不得静默显示/导出混合版本，不得生成正文全为原始版的 `refined-incomplete` 文件，不得让完整覆盖掩盖故障事实，不得把旧会话的未知状态伪装成无故障，也不得把完整 transcript 暴露给 renderer。 | J1/J2/J10/J15b/J15c；确定性长详情前置 + 两小时 J8。 | J15b/J15c 历史路径已达到联合验收完成：列表/keyset 分页、整场 `N/M`、独立故障事实、零/空/旧会话语义、三格式分版本导出及 `[原始版回退]` 均有回归；真实 packaged Electron 已执行会话 A 精修版跨页/导出，再切换会话 B 自动恢复原始版/导出。 |
| **SEM-F12** | 暂停、恢复与崩溃 | pause/resume 保留同一会话；迟到结果按 revision/watermark 处理。ASR 或 storage worker 重启后，已提交字幕事实可恢复；Agent 待办后续必须可幂等重放。运行 worker 故障、活动 media track 结束或系统挂起后，即使用户不点 Retry/停止，也必须主动释放采集资源；分别发布稳定事实 `AUDIO_TRACK_ENDED` 或 `SYSTEM_SUSPEND`。设备重新出现或系统唤醒不得自动重新采集，只有用户明确 Retry 后才可沿同一会话/cursor 重建监听链路；Retry 后必须再次产生字幕并继续满足 SQLite、sequence 与 transport 边界。native worker 正常收束先给 30 秒 graceful window；超时后只能终止并等待该 exact child，reap window 为 5 秒。字幕应用运行时以 45 秒作为优雅收束结束/升级触发线，ModelManager 的 5 秒收束与其并行；升级后仍须等待 exact child 收殓，因此 45 秒不是硬退出上限。一次性实机 runner 最多等待 exact Electron 120 秒；超时必须判失败，且清理只能作用于它启动的 process object。 | 不允许为了恢复接受跨会话事件；不允许在 error 状态继续占用 mic/loopback；不得在设备恢复或系统唤醒后自动重新申请采集权限；旧 native worker 未确认退出前不得启动 replacement generation；不得按进程名批量终止 Electron；不得用“硬总上限”绕过 exact-child exit 确认；Agent 恢复不得反向阻塞字幕恢复。诊断通过、操作者 completion、退出旁证通过或未再次复现都不等于实际设备移除/系统睡眠、根因定位、I3 或 I4 验收。 | J5/J6 + DB1 + I2 故障 smoke；I2 `device-removal-retry` / `sleep-wake-retry` 实机交互报告；真实模型活跃生命周期诊断；受监督产品壳 role evidence；exact-process timeout 与退出绑定回归。 | realtime/refine/storage UtilityProcess 已接 fatal `error`、角色标签和 exact-child exit 屏障；Coordinator 在旧世代 retirement 完成前禁止 replacement generation。2026-08-01 真实 loopback pause/refine 与 exact realtime worker 强制终止+Retry 均通过；后者证明同一 session/cursor、复用 adapter、新 worker generation、故障前后 final/refined 与零 transport 损失。设备移除与睡眠/唤醒的确定性边界已有局部回归；schema-v2 runner/verifier 现为实现完成·尚未验收，能够等待真实外部动作并闭合故障、采集释放、无自动重采集、显式 Retry、同会话字幕恢复和 SQLite/sequence/transport 边界，但尚无实际设备移除或系统睡眠报告。另一次未纳入权威 bundle 的 `PostQueuedCompletionStatus(6)` 仍无 native stack，故历史异常根因未定位。性能、原生拖动、I3/I4 与 Agent 仍待。 |
| **SEM-F13** | 独立增强文本 | LLM 修饰结果必须作为 `enhanced_transcript` 独立保存，声明 `sessionId`、输入水位、input digest、provider/model；权威转写始终可单独查看和导出。A2 首版只做会后或用户主动触发的整场增强。 | 增强文本不是 refined，不得覆盖、删除或伪装成 ASR 正文；失败不得改变字幕；按 committed segment 滚动增强不进入首版。 | J3/J5/J7 + artifact schema/版本测试。 | 已决定；D5 的正式 `enhanced-transcript@1` UI-free 后端纵切为实现完成·尚未验收：直接读取冻结整场字幕快照，全部分块与有界归并成功后只原子提交独立 `paragraphs[]` 产物，归并失败不留下部分产物；正式触发、preload/IPC、renderer 与版本化用户旅程仍无产品证据。 |
| **SEM-F14** | 永不保存原始音频 | 原始 PCM、录音片段或可回放音频文件只允许在有界内存缓冲中服务实时 ASR/精修，使用后释放；产品、诊断和 smoke 现在及未来都不持久化现场采集音频。模型归档即使附带上游示例音频，产品安装也只能提取 manifest 白名单运行文件。精修故障结构化日志只允许稳定错误码、处理阶段、会话内相对时点和不含正文的计数；不得记录原始 Error/stack。日志采用本地滚动 JSONL，最多 5 个文件、每个 1 MiB、最长保留 7 天，任一上限先到即清理；不自动上传，手动诊断导出入口后置且不阻断当前 MVP。所有 I2/I4 音频 child、交互报告与 summary 也只能保存指标、枚举和哈希。 | 不允许在 SQLite BLOB、应用数据目录、日志、导出、崩溃恢复文件、测试产物、模型目录或 Agent 上下文中保存现场音频；不预留“以后打开录音”的隐藏路径。测试语料的 generator/reference 可以受跟踪，但生成 WAV 必须忽略且不得提交。跨时钟诊断、精修故障日志和 I2/I4 报告不得保存绝对单调时刻、校准偏移、设备名、字幕正文、本地绝对路径、现场音频文件名/路径或原始错误文本；不得自动上传诊断日志或无限保留。 | J12 + J15c 故障日志内容/轮转/无上传负证据 + 存储/schema/诊断接口审查 + J14 安装目录检查 + I2/I4 报告隐私负扫描 + I4 打包版数据目录检查。 | J15c 精修故障日志已实现受约束字段、5×1 MiB/7 天轮转、关闭时排空与无上传负证据；正式 ASAR/win-unpacked 继续确认没有模型张量或音频载荷。I2 schema-v2 交互报告、进度、completion 的闭合字段和隐私负向契约现为实现完成·尚未验收；I4 两份来源隔离的音频 child、strict summary、移交包 manifest 及其严格隐私负扫描入口现为实现完成·尚未验收。移交包唯一含字幕正文样例的文件是受跟踪、固定 SHA 的合成 legacy reference fixture；manifest 明确区分该 fixture 与现场采集/报告正文，后两者仍固定为零。尚无专用干净机音频报告，I4 打包态真实 `userData` 检查尚未达到实机验收完成。 |
| **SEM-F15** | 内容型 Agent 边界 | Agent 首版只允许通过宿主端口读取已提交文本和本地个人记忆、调用受控 `ModelGateway`、运行有界 Agent Loop，并由宿主把结构校验通过的结果写入内部 Agent 派生表。调试聊天只能调用允许清单中的读取工具或请求固定后台任务；会产生新产物或云端费用的工具调用必须先展示执行预览并由用户确认。 | 禁止 shell、进程启动、任意文件读写、任意网络请求和外部服务写操作；Agent、插件或专用子 Agent 不得直接访问 SQLite；会议待办不能自动执行。 | J7/J13/J22 + 插件能力清单与 Tool Calling 审计。 | 已决定：SEM-F29/J23 隔离入口窄切片已达到联合验收完成；D4 正式字幕上下文、受控 `ModelGateway`、Pi Agent Loop、结构校验和宿主原子提交的纪要后端子边界，以及 D5 由同一宿主通过 `ArtifactWriter` / `MemoryCandidateSink` 分流提交三项固定任务的 UI-free 后端纵切，均为实现完成·尚未验收。个人记忆读取、正式 preload/IPC、renderer、执行预览与完整 J7/J13/J22 仍无产品证据。 |
| **SEM-F16** | 可插拔 Agent 能力 | Pi 低层 Agent 核心外必须有项目自有插件宿主；字幕通过只读字幕上下文插件接入，增强文本、会后结构化纪要、记忆提取与记忆合并分别作为第一方能力。专用子 Agent 由宿主按固定 recipe 创建，使用隔离上下文和固定能力集合；宿主领取后台 Agent 任务时必须把当前已装载且可执行的固定任务闭集交给 storage worker，未装载能力保持排队且不得调用 Agent 模型 provider；字幕系统本身仍独立运行。 | 不直接嵌入完整 coding-agent 扩展运行时；不让 Pi 插件拥有音频采集、ASR、字幕会话或字幕数据库写权；不提供任意提示词驱动的通用子 Agent 创建工具。 | A1 探针 + J3/J7/J13/J21/J22；插件卸载/失败时字幕 J1 仍成立。 | 已决定：SEM-F29/J23 隔离切片已达到联合验收完成；D4 的正式 `transcript-context` / `meeting-minutes` 与 D5 的 `enhanced-transcript` / `memory-extraction` / `memory-consolidation`、固定 recipe、已装载任务闭集、权限和运行中卸载隔离，以及 D6 的真实 storage utility transport 子边界均为实现完成·尚未验收。`memory-consolidation` 只是记忆任务内的确定性合并阶段，不创建第四项后台 Agent 任务；D6 中 PluginHost/Loop 仍在调用进程运行，不冒充 Agent utility。正式字幕提交边界、Agent utility 与 UI 读取链路仍无产品证据。 |
| **SEM-F17** | 本地模型资源闭环 | 核心字幕 ready 只依赖受审计、可恢复安装的临时字幕识别器、权威识别器与 VAD。精修模型默认不下载；用户只能在未监听状态下先通过独立下载动作按需安装，模型就绪后再通过全局精修偏好明确启用，且只影响下一次会话，无论来源是 `mic` 还是 `loopback`。缺失模型时尝试开启必须保持关闭并提示下载；安装完成也不得自动启用。用户可取消精修模型下载并保留合法 `.part`，但取消、应用重启或网络恢复都不得自动续传，只有再次明确点击“继续下载”才发起网络。精修开启偏好可跨重启保留，但每次启动都必须重新验证精修模型的就绪证明；缺失或损坏时把开关明确回落为关闭并通知用户，重新下载成功后仍须再次明确开启。运行中的精修 worker 故障不得修改全局偏好；关闭偏好不删除模型或旧会话精修稿。缺失、下载失败或运行失败不得阻止核心字幕、首次 `final`、历史和导出。 | 不得把可选精修模型纳入核心字幕 ready，也不得在两项实时识别资源与 VAD 任一缺失或损坏时继续开始会话；不得由精修开关隐式触发下载，也不得在下载成功后自动开启；不得在取消或重启后自动恢复网络；不得因单次 worker 运行故障自动关闭未来会话精修；不得在模型失效后静默显示为开启；活动会话中不得安装精修模型。活动会话期间允许修改全局精修偏好，但不得改变本会话开始时已经冻结的精修策略，修改只影响未来会话；renderer 仍不能提供 URL、哈希、路径或解压参数。 | 核心与可选资源分别覆盖 J14/J15c + B5/I4；真实模型调用沿用/扩展 I2。 | 临时字幕识别器、权威识别器与 VAD 三项核心 ready，以及精修模型独立供给、无隐式网络、两次显式动作、全局偏好、取消/继续和跨重启复核均已达到联合验收完成；schema-v4 packaged 首启/复启分别证明三项核心 marker 与四项总资源。I4 公网真实资源仍待实机验收。 |
| **SEM-F18** | 打包字幕 MVP | B5 的目标是 Windows x64 打包态确定性资格：正式包使用 ASAR、单击式当前用户 NSIS 和 `asInvoker`；只打入产品源码与生产依赖，模型不入包；整个 sherpa 平台目录解包以保持 `.node`/DLL 相邻；打包态禁用开发缝并开启 ASAR/fuse 约束。测试 package variant 必须与正式布局分离并声明不是 release candidate。完整产品载荷身份按 ASAR 实际运行时 `src/` 文件闭合，构建期 `.d.ts` 类型声明不进入 ASAR，也不得只在本地身份中计入。 | `win-unpacked` 可启动、文件存在或测试 variant 通过都不等于精确 NSIS 的干净机 I4；native 文件在磁盘不等于 utility 实际加载；unsigned 可安装不等于可公开可信分发。 | B5 package layout + packaged first/restart journeys + 两份 exact-child exit + 同轮/产品载荷 binding + NSIS lifecycle；I4 另验。 | 当前确定性资格由 revision `bbfd7041e5963e51942392323735298a7b81cb30` / run `31191838016` 绑定，已达到联合验收完成；远端 installer SHA=`d77d16c0…060c`、产品载荷 SHA=`e95fd87f…a35a`，下载 artifact 不含 installer 字节。历史本地候选 `d862c5fc…0de10` / `a1f03ed6…9accc` 只绑定旧产品载荷，不得冒充当前候选。I4 必须另行取得并核验 bbfd/run 的精确 installer 字节；干净机 I4 尚未达到实机验收完成或发布验收完成。 |
| **SEM-F19** | 程序卸载与用户数据 | MVP 程序卸载只移除程序、快捷方式和卸载登记，默认保留 `userData` 中模型、配置和 SQLite 历史；任何“删除本地数据”必须是以后单独、明确的用户动作，并限制在精确 userData 根。 | 不得让 NSIS 默认卸载静默删除字幕历史；“程序卸载成功”不等于用户数据已删除；保留数据也不等于卸载失败。 | B5 隔离 NSIS 机械生命周期 + I4 已产生 userData 后的卸载/重装验证。 | builder 固定 `deleteAppDataOnUninstall=false`；B5 只证明隔离 APPDATA 中与应用无关的哨兵不变。I4 非音频 runner 已改用 Known Folder 动态发现正式 userData，并比较 config/SQLite/legacy/三 marker 的卸载前后清单及离线重装复用；尚无专用干净机报告，因此真实数据保留尚未达到实机验收完成。 |
| **SEM-F20** | 固定高度字幕行流 | 字幕窗口不得因内容自动改变 bounds。文字按当前宽度自然换行；可见高度满后最旧完整视觉行退出、最新行保持在底部，跨段逐行淘汰，停顿时保留。`partial` 回改可重排，但布局永远不能修改识别文本或触发分段。一个段只要仍有任一视觉行可见，就仍属实时窗口内并可接受精修重排；当它的最后一条视觉行也从顶部完全退出后，renderer 必须把该段身份回报给主进程并从 canonical 实时视图永久淘汰，本会话内的迟到 `final/refined/translated`、精修故障回退、窗口放大或 renderer reload 都不得使它复活。该回报只能包含 schema、会话 ID 和段 ID，不得包含字幕正文、视觉行文本或屏幕几何。会话启用精修后，迟到精修稿自动更新仍可见的已定稿段并允许其重新换行；不得修改当前 `partial`。精修 worker 故障确认后，所有仍可见的已定稿段立即恢复各自首次稳定转写并在相同固定视口内重新排版；当前 `partial` 原样保留且仍优先，已淘汰段不复活，后续段继续显示首次稳定转写。当前字幕会话继续。MVP 运行中不弹精修故障提示、不改变右上角颜色或窗口高度；停止后工具条在既有 bounds 内显示会话状态通知并提供“查看历史”；该通知仅报告精修处理状态，不得概括或改写字幕内容。不同状态与颜色的后续提醒不阻断当前 MVP，未来必须另行冻结状态矩阵、非颜色冗余通道和后端契约，不能由 renderer 自行推断。 | 不使用横向跑马、自动增高、整窗清空、可见滚动条或按视觉宽度强制 VAD/final；逐行视觉退出不表示裁切或改写权威原文，但已经整段退出的实时显示身份必须被记住，不能只依赖会随重排变化的 CSS 裁剪；右上角状态标识变色不在本次范围；历史中的 `[原始版回退]` 不进入实时字幕正文；工具条会话状态通知仅报告精修处理状态，不是字幕内容摘要，也不得概括或改写字幕内容；故障回退不得覆盖或清空当前 `partial`；运行中无打扰回退不等于永久隐瞒故障。浏览器 zoom、离屏截图、隐藏窗几何或同一显示器重复声明不得冒充真实系统 DPI/异缩放双屏。 | DOM/reducer 固定 bounds 旅程，覆盖多字号、中英混排、长词、回改、跨段、停顿、视口未满不提前淘汰、整段视觉退出回报、迟到 `final/refined/translated` 与 reload/故障回退不复活、所有可见 final 的即时恢复与 `partial` 不变 + J15a/J15c。可见非音频 DWM 观察另以当前源码与 `package-lock.json` digest 绑定的严格报告覆盖真实 100/125/150/200% 系统缩放 × 深色/浅色/系统高对比 × 白底文档/深色视频/复杂桌面共 36 个组合，并至少一次在异缩放双屏间移动；每例明确确认透明面无黑底、文字可读、最新行完整、顶部无半行、无横向移动/滚动条、更新前后 bounds 不变，全程只注入合成 CaptionEvent 且不启动采集。最终 matrix verifier 必须重新读取全部 36 份 observation、逐份执行严格校验并核对文件 SHA 与组合摘要；只有 matrix 自身的 36 条声明而没有原始 observation 文件时必须 fail closed。 | 固定整行视口、顶部逐行淘汰、`partial` 优先、整段视觉退出后的 canonical 永久淘汰、迟到修订/reload/窗口放大/故障回退不复活、故障后全部可见 final 恢复及全程无 resize 已由 reducer、renderer→main IPC、canonical replacement、真实 Chromium 几何和产品壳旅程闭合，J15a/J15c 达到联合验收完成。可见非音频 DWM runner、单次 observation strict verifier、36 组合 matrix summarizer/verifier 与 fail-closed 契约测试为实现完成·尚未验收；尚无绑定当前候选的 36 例实机 matrix 报告，DPI、主题、透明窗与异缩放双屏移动尚未达到实机验收完成。 |
| **SEM-F21** | 同源两阶段实时识别 | 每个会话仍只选择一个 `mic` 或 `loopback` 来源并只采集一份 PCM。实时 worker 必须把同一份已通过 VAD 边界的有界内存样本同时送入临时字幕识别器与权威识别器：批准的双语 Zipformer 仅发布低延迟 `partial`；现有 X-ASR 独占首次 `final`，并继续作为首次稳定转写、SQLite、历史、导出与可选精修的唯一基础。权威识别器一旦为当前段发布非空结果，后续 `partial` 立即切到权威结果，临时字幕识别器不得回写；段结束时只允许权威识别器形成 `final`。临时字幕识别器的缺失或损坏使核心字幕 ready fail closed；启动失败必须以 `DRAFT_RECOGNIZER_START_FAILED` 使会话启动 fail closed。会话运行中临时字幕识别器故障时必须发布 `DRAFT_RECOGNIZER_FAILED` 并显式降级为仅使用权威识别器，当前会话继续。两个错误码的结构化诊断只允许阶段与不含正文的计数。权威识别器故障仍沿 SEM-F12 进入可重试 error。两者都只能使用有界内存，段结束、故障或停止后释放。 | 不得新增第二个音频来源、第二次采集或来源切换；不得让临时字幕识别器产生 `final/refined`、开启新段、写 SQLite/日志/导出/报告正文或覆盖首次稳定转写；不得把运行中临时字幕识别器降级冒充完整两阶段路径；不得因临时字幕更快而移动 SEM-T14 的冻结起点或 `<1000ms` 门槛。 | J16 + J1/J2；确定性层覆盖同一帧扇出、权威接管、缺失/损坏/`DRAFT_RECOGNIZER_START_FAILED` 启动 fail closed、`DRAFT_RECOGNIZER_FAILED` 运行故障显式降级、权威故障可重试、停止释放与 SQLite/历史/导出/日志/报告零临时字幕正文；模型供应与设置 UI 沿 J14，真实模型性能仍沿 I2/I4。 | 联合验收完成：`zipformer-bilingual-zh-en-2023-02-20` 临时字幕识别器、`x-asr-160ms` 权威识别器、同帧扇出、权威接管、稳定故障事实、J16 跨模块旅程及 schema-v4 四资源 packaged 首启/复启已闭合；真实张量、物理来源性能和干净机发布仍沿 I2/I4，未由确定性证据冒充。 |
| **SEM-F22** | 窗口拖动、穿透与前台层级 | 字幕窗解锁时，除工具条当前实际可见轮廓和卡片内侧 `8px` 拉伸带外，整张可见字幕卡都必须可用主键按住后立即拖动；没有指针位移的按下再松开不得改变窗口 bounds，也不增加定时长按或额外移动阈值。字幕窗命中顺序固定为 `20 DIP` 透明外边距穿透、工具条实际轮廓穿透、`8px` 拉伸带、普通拖动区域；工具条轮廓与拉伸带重叠时由工具条先接管。工具条实际轮廓与卡片内侧拉伸带之间必须保留至少 `8 DIP` 的普通拖动区间；轮廓上边、右边及其相邻普通拖动区的原地点击或轻微抖动不得发起拉伸，反复操作不得累计改变字幕窗宽高。字幕窗四周透明外边距不得成为不可见抓取区。工具条排除区必须随真实渲染轮廓变化，不能用工具条 `600 × 72 DIP` BrowserWindow 或常态固定大洞代替；布局尚未就绪、renderer reload、报告失效或收到非法矩形时，必须临时回落到 `584 × 64` 最坏尺寸穿透区，取得同代有效矩形后立即收缩到真实轮廓。工具条无论锁定状态都只由现有明确握把发起拖动：握把必须使用两列三行六点形状，视觉图标置于既有 `24 × 30 DIP` 命中盒内；解锁时握把移动字幕与工具条组合，锁定后字幕窗保持恒穿透且不能拉伸，握把只移动工具条。设置窗与字幕历史的可拖区域只限各自 `48px` 顶部标题栏并排除其中全部交互控件，正文空白区不隐式拖动；两窗获得焦点时临时进入与字幕/工具条相同的 `screen-saver` 层并置于其上，失焦时立即恢复普通层级，不永久置顶。两窗标题栏共用主题感知的中性加深表面语义 token 与底部分隔线；该颜色只表达窗口结构，不表示运行状态。所有拖动仍走主进程手动 bounds 更新，并覆盖 `pointerup`、`pointercancel`、`lostpointercapture`、renderer blur/destroy、重复开始、锁定变化以及应用最小化/恢复的收尾。应用最小化必须取消而不是续接当前手势，同时推进窗口交互代次、停止主进程拖动/拉伸并把字幕窗和工具条设为原生鼠标穿透；恢复窗口集合、bounds 与前台层级后，必须要求同代 renderer 按当前局部 DIP 指针位置重新执行命中判定，即使指针静止也能让解锁字幕卡在下一次主键按下时立即拖动。renderer reload 必须重放当前窗口交互代次；过期代次的穿透、拖动或拉伸意图一律拒绝。锁定字幕窗始终穿透；字幕窗在工具条轮廓内穿透后由工具条自身按真实轮廓接管。 | 不把设置或字幕历史正文做成整页隐藏抓取区；不让透明外边距拦截桌面；不在有效工具条轮廓到达后继续保留最坏尺寸断续命中；不让工具条状态文字、按钮或控件间隙承担隐藏拖动；不得因更换握把图标扩大拖动入口、改变命中盒、拖动时机、窗口 bounds 或工具条外接轮廓；不以固定长按延迟、额外移动阈值、永久 always-on-top、自动隐藏字幕或自动搬移设置窗替代该交互；标题栏加深不表示选中、警告或成功。不得依赖最小化前手势收到 `pointerup`、`pointercancel` 或 renderer blur，也不得依赖恢复后的下一次 `mousemove` 才恢复交互，不得把 `resume` 的首次命中确认排队到 rAF，也不得让主进程改变窗口几何后仍沿用移动前的局部指针命中，或在恢复时把整个透明字幕 BrowserWindow 临时变成实心命中面。工具条轮廓和恢复时的指针坐标只服务有界内存中的窗口交互，不得写入日志、数据库、导出或证据报告。 | J17/J19 确定性联合旅程 + I2 `dwm-drag` 可见实机观察。确定性层必须覆盖动态轮廓变化、工具条边缘及相邻普通拖动区反复点击不改变宽高、手动拉伸或组合拖动结束后按当前局部指针重新命中、首帧/重载/非法或陈旧矩形的最坏尺寸回落、透明边距/拉伸带/工具条/可见卡片命中矩阵、原地点击零位移、全部取消路径、两列三行六点握把与既有命中盒、握把主键按下立即开始且原地松开 bounds 不变、图标替换前后工具条实际轮廓不变、设置与字幕历史的焦点层级往返、`48px` 标题栏/交互控件排除、深浅色/高对比共享 token，以及缺失 renderer 收尾事件时的手势重置、当前代次校验、renderer 命中结果和主进程原生穿透调用意图；确定性层不得声称证明 Windows 已实际应用原生穿透或恢复后真实鼠标拖动。I2 `dwm-drag` schema-v5 实机层必须观察真实任务栏恢复、DWM 层级、原生穿透、恢复后首次鼠标拖动连续性、工具条边缘反复操作稳定性、几何变化后当前指针重命中、恢复后工具条附近首次拖动稳定性与 DPI/异缩放下的命中。 | 既有 J17 动态轮廓、拖动/拉伸、零位移、取消路径、握把、标题栏主题与焦点层级往返，以及应用最小化/恢复窗口交互代次、静止指针命中判定、过期意图拒绝与 renderer reload/crash 降级已达到联合验收完成。本轮工具条边缘防误拉伸、原生拉伸关闭、`resume` 同步命中与几何变化后同代重命中增量已由 J17/J19 的真实 renderer 联合旅程、主进程控制器测试与 BrowserWindow 创建契约覆盖，达到联合验收完成；I2 `dwm-drag` schema-v5 入口与 12 组合矩阵为实现完成·尚未验收，尚无当前候选实机证据。 |
| **SEM-F23** | Fluent 2 桌面界面一致性与响应 | 四个可见 renderer 必须在不改变字幕事实、会话状态或窗口命中语义的前提下共享 Fluent 2 导向的字体、间距、形状、图标、表面、焦点和动效 token。字幕窗与工具条必须保留轻量、稳定的直接 DOM 热路径；设置窗与字幕历史可以使用 React 组织视图，但只能消费受限 preload 暴露的权威状态与命令回执。工具条作为跨背景悬浮控制面，必须在深色、浅色与自动主题下保持同一套深色半透明表面、浅色普通按钮与握把、交互底纹和 phase 色调；Windows 系统高对比可以接管颜色。`barColor` 只改变字幕背景，`toolbarOpacity` 只改变固定工具条表面的透明度，二者不得改变工具条按钮前景。设置窗与字幕历史使用适合长期窗口表面的 Mica，系统不支持、窗口失焦或高对比模式下回落为可读的不透明中性表面；不得把大面积 Acrylic 或 CSS backdrop filter 作为常驻背景。所有可执行控件必须有键盘路径、可见焦点、可及名称和不少于形状或文案的一条非颜色状态通道；动效须短促、直接且遵守 `prefers-reduced-motion`。用户输入必须立即得到按下/忙碌反馈，实际成功仍以 `CommandResult` 或权威快照为准；外观偏好允许本窗即时预览并延迟持久化。 | React、TypeScript、Vite 或 Fluent 图标是实现选择，不形成新的字幕事实、Capability 或存储权威；不得因框架迁移把 `partial` 放入持久化/全局业务状态、重建整棵字幕 DOM、乐观伪造命令成功、改变 SEM-F20 固定高度行流或 SEM-F22 命中顺序。不得让普通主题或字幕自定义背景色翻转、染色或降低工具条基础控件的可见性；不得用无限装饰动画、持续大面积模糊、隐藏拖动面、纯颜色错误态或组件库默认视觉替代产品语义。 | J18 确定性联合旅程；字幕布局继续由 J15a，窗口抓取与 DWM 层级继续由 J17/I2 `dwm-drag`。确定性层必须从生产 renderer bundle 加载四窗，覆盖深色/浅色/自动主题下工具条计算样式不变、`barColor` 只作用字幕、`toolbarOpacity` 只作用工具条表面、系统高对比、键盘焦点、reduced motion、设置 pending/失败/权威回落、字幕历史版本与分页、renderer reload，以及开发入口和生产入口隔离；实机层复用 J15a/I2 观察真实 Mica、系统 DPI、异缩放和连续拖动。 | 既有 J18 生产四入口、TypeScript/React renderer、共享 token/固定 Fluent System Icons、Mica 回落、键盘焦点、reduced motion 与设置/历史旅程已达到联合验收完成；本次工具条主题独立配色、`barColor` 字幕专用与 `toolbarOpacity` 工具条表面专用增量由 UI 定向测试及绑定当前产品载荷的 schema-v6 产品壳覆盖，已达到联合验收完成。真实 Mica 合成、DWM、DPI、异缩放与连续鼠标观测仍为实现完成·尚未验收，继续沿 J15a/I2，不由该状态冒充实机验收。 |
| **SEM-F24** | Windows 任务栏与应用生命周期 | 工具条必须作为字幕系统唯一持续存在的主任务栏窗口，使用稳定的应用标题并允许最小化；不可聚焦的字幕覆盖窗继续不单独占用任务栏。用户从工具条或 Windows 任务栏最小化主窗口时，字幕覆盖窗和当时可见的设置/字幕历史一并离开桌面，但进程、当前会话、互斥音频来源、字幕持久化与 RuntimeSnapshot 均继续；从任务栏恢复或再次启动同一单实例时，必须恢复最小化前的可见窗口集合、窗口 bounds 和前台层级，不创建新会话。最小化事务必须终止所有进行中的窗口手势、推进一次窗口交互代次并使旧代次失效；每次任务栏恢复或第二实例恢复事务必须再推进一次，并在该事务内用同一新代次完成 `suspend → resume`，在窗口几何与层级恢复后按当前指针重新执行命中判定，保证解锁字幕窗无需先移动鼠标即可由下一次新的主键按下立即拖动，同时透明边距和锁定字幕窗继续穿透，字幕窗内的工具条轮廓由工具条接管。关闭设置或字幕历史只关闭对应辅助窗口，主任务栏入口必须继续存在；工具条“退出”、Windows 对主任务栏窗口的关闭以及正常应用退出都必须进入 SEM-F12 的退出序列，停止接收命令、释放采集、提交或标记中断会话、收束 worker/SQLite 后关闭全部窗口，并让 `npm start` 的 exact-child supervisor 自然返回。退出收束失败不得伪装为 clean exit，也不得在主任务栏入口消失后留下无法恢复的后台实例。 | 最小化不等于暂停、停止或退出，也不表示恢复后续接最小化前已按下的指针手势；关闭辅助窗口不等于退出字幕系统；不新增系统托盘常驻、自启动或多实例。确定性产品壳中的合成指针与原生 API 调用意图不证明 Windows 实际应用鼠标穿透或完成真实鼠标拖动；强制按进程名结束 Electron、跳过 exact-child 收殓或把非零退出改报正常退出仍被禁止。 | J19 确定性联合旅程 + J17 窗口交互代次断言 + exact-child exit evidence；真实任务栏恢复后的原生穿透、首次鼠标拖动与 DPI/异缩放行为由 I2 `dwm-drag` schema-v5 补充，既有 I4 非音频门禁不承担该新增 DWM 结论。失败路径必须注入窗口最小化/恢复异常、同步确认超时、指针坐标不可用、原生穿透设置失败、renderer reload、过期窗口交互代次及退出异常，固定诊断只含 `role/code`；字幕窗保持隐藏或穿透，工具条保留可访问主任务栏入口并允许重试，不得误报 clean exit。 | 既有 schema-v6/schema-v7 产品壳、J17/J19 联合旅程及 exact-child 证据已覆盖窗口交互代次，达到联合验收完成。本轮 `resume` 同步命中及恢复后工具条附近静止指针首次拖动稳定性增量已由 J17/J19 真实 renderer 联合旅程覆盖，达到联合验收完成；I2 `dwm-drag` schema-v5 为实现完成·尚未验收，真实任务栏恢复后的连续鼠标、DWM、DPI 与异缩放观察尚无当前候选实机证据。 |
| **SEM-F25** | 识别 provider 与 Agent 模型 provider 分离 | 识别 provider 与 Agent 模型 provider 必须独立选择。本行把 SEM-F21 的适用范围局部收窄为纯本地权威识别；每个新会话只允许冻结一种权威识别策略：纯本地权威识别，或云端主力识别与本地降级。云端主力识别仍要求核心字幕模型资源包先通过本地就绪证明；本地识别器保持就绪但不持续解码，云端流在正常期间负责该会话的 `partial` 与唯一首次 `final`。只有明确断开、稳定 provider 错误或宽松的连接存活检测失败才能触发降级；普通延迟波动不得触发。降级时允许把当前有界内存 PCM 交给本地链路补齐当前段，随后在同一会话内单向使用本地识别，不自动切回云端。策略和已确认关键词快照只在新会话开始时读取一次。正式 Agent 首版按 ADR 0011 只要求闭合 Agent 模型 provider 的 registry、受信任非敏感配置、启动环境凭据状态、冻结和降级：非敏感 DeepSeek 配置默认使用 `deepseek-v4-flash`，API key 只从启动环境读取；真实 DeepSeek 公网调用与本地 Agent 模型 provider 实现后置，不阻断当前确定性联合验收。 | 不把两种权威识别策略称为监听模式；云端不是唯一主力路径，纯本地权威识别必须独立成立；不得同时运行两套权威解码争抢 `final`，不得因慢响应频繁切换，不得在活动会话中更换策略，也不得为降级持久化 PCM。Agent 模型 provider 的选择不得改变识别 provider；这些内部边界不表示真实供应商、账户、配额、模型质量或公网已验收。 | J20 + J1/J2/J12/J14/J16；云端识别 provider 使用契约替身，真实公网/provider 另走 I4，物理来源性能仍走 I2。正式 Agent 模型 provider 的内部 registry/config/冻结/降级走 J7/J13/J21/J22/J24，外部模型推理使用契约替身。 | 已决定；识别 provider 抽象、云端识别与降级尚无实现证据。D9 的 main-only bootstrap/catalog、D10 的 registry/Gateway/Pi 内部路径及 D11 的 ConfigStore v2 Agent 设置存储子边界均为实现完成·尚未验收；正式 main 对启动环境的最早读取与清除、真实 child/Agent utility、网络适配器和完整产品链路仍无实现证据。 |
| **SEM-F26** | 本地个人记忆 | 个人记忆偏好首次默认开启，但只有 SEM-F28 的 Agent 总开关与个人记忆同时生效、Agent 处理资格为 `ready`，且终态会话同时位于 `automaticProcessingSince` 与 `memoryProcessingSince` 之后时才静默执行结构化提取。Agent 总开关或个人记忆每次从不生效转为生效时写入新的个人记忆自动处理边界；任一关闭时该边界失效，重新开启不得自动补处理关闭期间或更早的会话，但用户可在当前开关与资格满足时从历史明确请求重新提取。总开关关闭或资格不满足时，个人记忆偏好保留但不创建或领取记忆任务。所有记忆始终保存在现有 SQLite 中，由同一 storage worker 写入；权威原始转写仍是唯一完整正文，记忆只保存原子结构、来源事件范围、水位和 digest，不复制整场正文。提取结果按长期结构化记忆、会话经历记录、丢弃三层筛选；明确决定、结论和待办即使只出现一次也可进入长期结构化记忆。自动推断必须区分明确偏好与推断倾向，保留来源、范围与可解释的置信/价值等级；冻结字幕快照没有说话人身份，因此 `memory-extraction` 提出的全局个人偏好无论标记为 `explicit` 还是 `automatic` 都必须丢弃，只有独立的用户明确确认动作以后才能建立全局偏好。首版只使用 SQLite 结构化字段、规范化关键词和别名匹配，不创建 FTS5、图数据库、向量数据库或语义索引。Agent 内部读取只接受受信任宿主给出的 exact 范围、类型、语义键及条目/完整序列化字节预算，按 scope/item 生命周期和当前 revision 过滤并固定排序；单次最多扫描 256 个候选、返回 20 条/65536 canonical JSON UTF-8 字节，每条最多 8 个无正文来源引用，条目只能整条纳入且省略时必须显式标记。全局关闭个人记忆后停止新提取、检索注入和下一新会话的记忆关键词，读取返回零条目休眠投影；既有条目留在本地且不因开关批量改写或自动删除，重新开启后可再次有界读取。 | “全量进入记忆”表示 Agent 处理资格为 `ready` 且同时位于 Agent 与个人记忆自动处理边界内的终态会话接受筛选，不表示复制完整正文或无差别保留噪声；不得复用旧边界补跑个人记忆关闭期间的会话，不得让记忆成为第二份权威正文，不得把 Agent 摘要再作为记忆提取来源，不得允许模型/renderer 传设置、自由文本、SQL、任意排序或 cursor 绕过读取边界，不得截断单条记忆伪装成完整注入，不得因关闭个人记忆或 Agent 删除字幕历史，也不承诺首版逐会话敏感标记。 | J21 + DB7/J12/J24；本地与云端 Agent 模型 provider 只替代模型外部边界，内部 storage/job/plugin 使用真实实现。 | 已决定；D3 的记忆 schema、原子候选提交、去重/冲突/revision/suppression 存储子边界，以及 D5 从冻结权威转写直接提取、任务内确定性合并、`MemoryCandidateSink` 提交和“单条个人记忆删除 → 各旧来源 digest suppression → 物理移除当前内容”的幂等 UI-free 后端纵切，均为实现完成·尚未验收。D5 还覆盖无身份的显式/自动全局偏好拒绝；D8 已登记有界检索与休眠投影但尚无实现证据，正式读取/删除 IPC 与确认关键词仍无产品证据。 |
| **SEM-F27** | 确认关键词与个性化边界 | 自动提取的术语、实体名、别名和偏好只能先形成带来源的候选。只有用户明确确认的词汇型条目才能影响后续 ASR，并按少量全局关键词加用户选择的主题/项目关键词组成会话开始时冻结的快照；provider 不支持受控关键词时必须显式暴露能力缺失，不得用会后字符串替换改写首次稳定转写。写作、摘要和表达偏好只影响 Agent 产物。用户明确确认或人工修正的内容只能由用户再次明确修改；自动推断可以补充证据或形成冲突候选，但不能覆盖当前明确值。 | 不得让静默推断直接改变 ASR，不得把任意个人偏好塞入识别上下文，不得把关键词匹配称为说话人识别，也不得让后到的自动推断覆盖用户确认内容。 | J20/J21；覆盖支持/不支持关键词的 recognition provider、范围冻结、冲突与下一会话生效。 | 已决定；尚无关键词确认、范围选择或 provider 能力接线证据。 |
| **SEM-F28** | 后台 Agent 任务、专用子 Agent 与调试聊天 | Agent 总开关首次默认关闭；个人记忆开关仍按 SEM-F26 默认开启，但只有 Agent 总开关开启且 Agent 处理资格为 `ready` 时才生效。总开关开启时持久化新的 `automaticProcessingSince`；Agent 总开关或个人记忆每次从不生效转为生效时另存新的 `memoryProcessingSince`，自动记忆任务必须同时位于两个边界内，个人记忆关闭期间或更早的会话不得在重新开启后自动补处理。自动请求遇到 Agent 总开关边界之前的终态会话返回 `outside_automatic_window`，更早会话只能由用户从历史明确请求；只落在个人记忆边界之外时，纪要与增强文本仍可对账，但省略记忆任务。Agent 处理资格只允许 `ready/no_committed_transcript/outside_automatic_window/agent_disabled/provider_not_configured/cloud_disclosure_required/credential_unavailable/local_model_not_ready/session_not_terminal`，并固定按“会话终态 → 已提交正文 → 自动处理时间边界（仅自动请求）→ Agent 总开关 → Agent 模型 provider 配置 → 云端披露/凭据或本地模型就绪 → `ready`”判定；用户请求忽略时间边界但不得绕过其它条件，明确请求记忆任务时个人记忆仍须开启。只有 `ready` 可以创建或领取后台 Agent 任务，其余结果不调用 Agent 模型 provider，并在设置或历史显示下一动作；领取还必须限制为宿主当前已装载的固定任务闭集，未装载任务保持排队。`MeetingStopped` 在完整提交水位确定后，为会后结构化纪要、个人记忆和增强文本创建彼此独立的 SQLite 后台 Agent 任务；三者直接消费同一 `sessionId + inputWatermark + transcriptVersion + digest`，任一失败不阻塞另外两项或字幕系统。零条首次稳定转写的终态会话不跨越字幕提交边界，不创建任务或调用 Agent 模型 provider；只要至少有一条首次稳定转写就不得按任意会话时长拒绝。`transcriptVersion='refined'` 只允许整场 `N=M` 的完整精修稿；精修覆盖不完整的混合显示正文不形成首版 Agent 输入版本，必须明确使用权威原始转写。输入超过单次模型上下文时，由宿主按 ADR 0009 优先在字幕段边界确定性分块；单段超预算时按 Unicode code point 范围完整分片，全部分块及归并成功后才允许提交，不得静默截断、只处理开头/末尾或提交部分产物。任务按固定 recipe、Agent 模型 provider、模型和输入快照持久化，可取消、可恢复；自动重试沿用 `runId`，用户主动重新生成创建新 `runId` 和产物版本。本地 Agent 重任务在活动字幕会话期间不得启动，已经运行时有界停止并稍后重试；云端 Agent 任务可继续。首版提供默认隐藏的基础调试聊天：只读取用户明确选择的终态会话和有界记忆，聊天记录单独保存在本地 SQLite 且不自动进入记忆。聊天 Agent 只能调用固定业务工具；写操作先确认，再由 Agent 插件宿主创建一层、固定能力和预算的专用子 Agent，结构校验通过后由宿主提交结果。选择云端 Agent 模型 provider 前必须明确告知终态会话正文将发送到该服务；确认后可自动处理，首版不提供逐会话敏感排除。正式首版默认从 main-only 受信任配置表读取 `deepseek/deepseek-v4-flash` 的非敏感参数，只从启动环境 `DEEPSEEK_API_KEY` 读取一次凭据；正式 main 必须在任何窗口、preload、renderer、worker、child 或 utility 创建前校验、复制并从 `process.env` 删除该变量，所有子进程环境显式排除它。初版只允许 exact origin `https://api.deepseek.com` 的受控路径并拒绝 redirect；应用不自动读取 `.env`，不提供 renderer 凭据写入接口。 | 不得让摘要依赖记忆提取或让记忆依赖摘要，不得让任务阻塞 `MeetingStopped`、退出或新字幕会话；不得在 Agent 关闭、配置不满足、披露未确认、启动环境凭据缺失或插件未装载时创建/领取不兼容任务；不得在重新开启后静默处理关闭期间或更早的会话；不得把精修覆盖不完整的混合显示正文冒充完整 `refined` 输入，不得把空会话、截断输入或部分分块伪装成完整成功；不得把调试聊天当作权威会话、自动记忆来源或正式通用 Agent；不得递归委派、直接写 SQLite、泄露内部思维过程、把 API key 写入配置/SQLite/日志/报告、通过子进程环境传播、携带到其它地址或开放任意工具。 | SEM-T07/J3–J7 + J13/J21/J22/J24；覆盖真实 SQLite/PluginHost/UI 与 provider 配置和凭据初始化，Agent 模型 provider 与网络使用契约替身。 | 已决定：SEM-F29/J23 隔离入口窄切片已达到联合验收完成；D3–D6 的正式存储、三项任务 UI-free 后端与 storage utility transport，D9–D12 的 provider bootstrap/registry、设置存储和字幕提交边界接线子边界均为实现完成·尚未验收；D13 正式任务存储网关接线已登记。三项任务共享冻结输入、独立提交、replacement 策略先行和同 `runId` 恢复已有子边界证据；正式 main/preload/renderer、Agent utility、provider 凭据启动接线、资源仲裁、用户读取/操作链路与调试聊天仍无完整产品证据，不能据此提升 J21/J22/J24。 |
| **SEM-F29** | 隔离 Agent 内核开发入口 | 首个 Agent 实现切片必须通过隔离 Agent 内核开发入口验证 Pi Agent Loop、静态第一方插件 registry、能力白名单、流式调试聊天、执行预览与用户确认、固定 recipe 专用子 Agent、后台 Agent 任务的 SQLite 持久化/取消/重试/恢复，以及项目自有 `ModelGateway`。入口使用独立 renderer、preload、IPC access policy、utility process、诊断目录、userData 和 SQLite；只允许读取由真实 storage worker 写入的无音频合成终态会话。首个真实 Agent 模型 provider 是用户配置的 OpenAI-compatible HTTPS 服务；本地 Agent 模型 provider 只冻结接口，确定性测试只在云端 Agent 模型 provider 外部边界使用替身。凭据只由 Agent 主进程经 `safeStorage` 保存，不能返回 renderer 或进入 SQLite、日志与报告。 | 不得导入或启动字幕主进程、音频采集、ASR、`MeetingStopped` 联动、正式历史/设置入口或个人记忆；不得把参考结构化产物称为会后结构化纪要、增强文本或记忆；不得把开发入口、Pi 依赖或其 renderer 产物放入正式安装包，也不得用 J23 冒充 J21/J22。 | J23 + A1 Pi/Electron 探针；使用真实 React/preload/IPC/PluginHost/job/storage worker/SQLite，仅 Agent 模型 provider 使用确定性契约替身；正式包布局作负断言。 | 联合验收完成：J23-B01–B16 已由真实 React renderer、受限 preload/exact IPC、Agent/storage utility process、Pi Agent Loop、静态插件宿主、`ModelGateway`、任务调度、真实 storage worker 与候选 SQLite 的跨模块旅程闭合；覆盖顺利执行与重启恢复、拒绝、claim 前后取消、408/429/5xx 同 `runId` 重试、鉴权/Schema/六类越权终止、应用中断、utility replacement、确认幂等、renderer reload、`safeStorage` 降级及严格隐私负扫描。确定性替身只位于 Agent 模型 provider、系统凭据存储、调度 gate 和强制中断外部边界；该结论不接入字幕提交边界，也不改变 J21/J22/J24。 |
| **SEM-T01** | 单元测试边界 | 单元测试只证明局部不变量，不能单独证明用户能力完成。 | “测试数很多”“覆盖率高”都不能替代用户旅程。 | 至少一条登记的确定性联合旅程。 | 规则已生效。 |
| **SEM-T02** | 联合测试边界 | 联合测试必须让多个真实产品模块围绕同一用户结果协作；只在声卡、网络、系统权限、云 provider 等不可确定外部边界使用契约替身。 | 全链路 mock、直接调用最终 exporter、只断言某函数被调用都不算联合旅程。 | CI 测试列出真实模块、替身边界和用户可观察结果。 | J1/J2 已按此执行。 |
| **SEM-T03** | 托管 CI 边界 | Windows hosted CI 证明确定性接线、回归、故障注入和打包机械资格，不声称证明真实 WASAPI、物理麦克风、DWM、模型性能、SmartScreen、交互权限/安装向导、两小时稳定性或“干净机”。锁文件依赖安装后必须显式安装锁定的 Electron runtime，并在任何 Electron、打包或回归步骤之前 fail closed 校验 `electron.exe` 存在且 runtime 版本与 `package.json`/锁文件一致；runtime 供给失败必须作为环境前置失败终止，不能误记为字幕布局或产品断言失败。一轮可下载的 CI 资格证据必须由最终索引绑定实际 checkout revision、触发时 `GITHUB_SHA`、run ID/attempt、workflow/job、lockfile/workflow digest、精确 installer SHA 以及布局/B5 binding/release layout/NSIS 报告 SHA；生成前必须确认实际 revision 与触发 revision 相同且受跟踪工作树无改动。`.artifacts` 点目录上传必须显式包含 hidden files，已有报告未被收集或成功 run 未产生 artifact 时必须 fail closed。确定性证据测试不得读取 Git 忽略的开发机模型目录：模型解析契约只能创建工作区内受控模型就绪证明 fixture，并明确保持不证明真实张量或模型性能。I3 文本源码 provenance 的全部输入必须由 `.gitattributes` 固定 LF；为兼容规则生效前的已有工作树，哈希前只允许把 CRLF 规范为 LF。完整产品载荷中的 `.js/.mjs/.json/.html/.css/.md` 也必须由 `.gitattributes` 固定 LF，使本机与 hosted checkout 在仍按精确字节计算时得到同一载荷；package、installer、ASAR、JSON 报告和其它二进制证据不得做内容规范化，仍按精确字节计算 SHA。可下载 artifact 中由 strict reader 按当前工作树复算的每个文本 provenance 输入都必须由 `.gitattributes` 固定 LF；caption layout 的 runner/verifier 与索引复算的 `package-lock.json`/workflow 也在此列，下载复核不得依赖 Windows checkout 的隐式 CRLF。 I3 非音频确定性 fixture 必须以独立 Node child 执行，并在该 child 加载 runner 与产品模块前通过进程环境把时区固定为 UTC，使 `toLocaleString` 这类外部时区边界可重复；该前置只作用于 I3 fixture child，不得污染同 lane 的其它测试或产品进程，产品导出的时间仍跟随运行它的 Windows 系统时区，实际导出字节及其 SHA 不得规范化。 | CI 绿色、静默 NSIS 安装成功或 `win-unpacked` 运行成功都不等于实机/发布验收完成；本地伪造同结构 JSON、只写分支名、只依赖 GitHub 页面状态或把 PR head 与默认 checkout 的 merge revision 混为一谈，也不构成精确提交绑定。索引不带签名，只有从对应 GitHub run 下载时才携带远端来源语境。受控模型就绪证明 fixture 只验证解析和边界，不得冒充真实模型调用。 | Electron runtime 安装/版本前置契约 + 模型 fixture 独立性契约 + I3/产品载荷 LF checkout 契约 + CI provenance 索引严格 verifier + workflow 契约；对应 I2/I3/I4 另有结构化实机报告。 | provenance writer/verifier、跨报告哈希复核、workflow 顺序和含 revision/run 的上传名已达到联合验收完成；本地合成 run 的当前候选交叉绑定探针通过。run `30750568366` 在 revision `2242103eb917f2afbfe81c7c8df788852bb36ebc` 因 runtime 缺失止于首个布局步骤；run `30760407160` 在 revision `0d0cd9f91cfd5136bbd5d3fec44e636da04b4e21` 通过 runtime、布局与 DB0 后暴露 DB1 fixture 漂移及 hidden artifact 未上传。run `30761472817` 精确绑定 revision `811fc6d14e927305588cf3e1603a9d09f7186e18`，模型 fixture 与 I3 文本 provenance 修复后，run `30763123116` 精确绑定 revision `2efb8969ec0608dd44695586ef70255f01c79fa7`；后者已通过 runtime、字幕布局、DB0/DB1/Gateway、四窗口产品壳、packaged 双启动、release NSIS 及隔离安装卸载，模型 fixture 与十个 I3 文本输入也已通过，但完整产品载荷因其余产品文本 checkout 行尾未固定而与本机候选漂移，末端 2 项 I3 证据测试失败，最终 provenance 索引按设计跳过。完整产品文本 LF checkout 修复与新候选 B5 重建为实现完成·尚未验收；仍须由新 revision 的完整 workflow 与 provenance artifact 验收。 run `30764235663` 精确绑定 revision `92a968a0bae405226fe7a9baa6fd1dd168f7cbe2`，已通过 Electron、布局、存储、产品壳、packaged、release NSIS 与隔离安装卸载前置，完整回归仅 I3 非音频 Markdown 导出 SHA 因本机与 hosted Windows 外部时区不同而漂移，最终 provenance 索引按设计跳过；I3 fixture child UTC 前置已由 run `30765231206` 在 revision `3b4b0db2f56f57e3d0e7500c77593b406a5dd857` 验证：完整 workflow、provenance 绑定与 artifact 上传均成功；artifact `qualification-evidence-3b4b0db2f56f57e3d0e7500c77593b406a5dd857-30765231206-1` 的 GitHub ZIP digest 为 `2df032ffb1c4d7f3da3130cce240b617559947f8b4cef0c63d8cf8b0ca33698c`。下载后 strict reader 因 caption layout runner/verifier 在 hosted checkout 为 CRLF、开发机为 LF 而拒绝当前工作树复算。两份脚本及索引复算的 lockfile/workflow LF checkout 修复随后由 run `30766172580` 在 revision `f05f3f9537d0200b63b2e28bb79846ced74f827d` 验证：全部 workflow steps 成功，artifact `qualification-evidence-f05f3f9537d0200b63b2e28bb79846ced74f827d-30766172580-1`（ID `8839039319`，GitHub ZIP digest `33bddcb065f5297ab9b244c60455dda798d5415abab1b213d0c1fac65a537017`）下载后由五个 strict readers 复核，四份报告 SHA、lockfile/workflow SHA 及跨报告绑定一致；41 个文件/29 个 JSON 的隐私负扫描无音频产物/引用、本地绝对地址、字幕正文或设备字段。下载包不含 installer 字节，同轮 workflow step 17 已在索引生成前复核精确 installer SHA。J9-CI 已达到联合验收完成；I2/I3/B5 证据仍不得冒充真实两小时声源、签名或干净机证明。 run `30786324179` 精确绑定 revision `c36aefaee4778a1bf2dfe1ee005924a724f4be53`：Electron runtime、字幕布局、存储、四窗口产品壳、packaged/NSIS 前置均成立，但 evidence lane 的 tracked I3 资格报告因 `scripts/i2-live-caption-player.html` 未被 `.gitattributes` 固定 LF 而在 hosted checkout 触发 `i2PlaybackPageSha256` 漂移；最终 provenance 索引按设计跳过，该 revision 的新增证据投影保持实现完成·尚未验收。 LF 修复 run `30787209338` 随后精确绑定 revision `f86ac1ef604dc7da0728c6eda44d59bbfd1e09bf`：全部 workflow steps 与 629 项回归成功，最终 provenance 索引及 artifact 上传成立；artifact `qualification-evidence-f86ac1ef604dc7da0728c6eda44d59bbfd1e09bf-30787209338-1`（ID `8845725648`，GitHub ZIP digest `b9d0a56b55fa4c6728be660698654bce418ee6364fd43a59eb1be9ccb9993242`）下载后通过五个 strict readers、四份报告与两个源码 digest、跨报告绑定及 41 文件/29 JSON 隐私负扫描。同轮索引绑定 installer SHA=`b9becb191234cefa6ddfba48bc2865379d6dc62f1a7020c85124df02f2516f31`；下载包不含 installer 字节。该精确 revision 的 J9-CI 已达到联合验收完成，且不扩大托管 CI 边界。其后 run `30790372286` 精确绑定 I2 模型替换决策 revision `5c6ce847fc07329802e3e98db9db70cc683f1f75`：Electron `43.2.0` 前置，workflow 结论为 `success`；core 414 tests=407 pass+7 expected model/Silero-asset skips，integration 27/27，evidence 190/190；总计 631 tests=624 pass+7 expected skips+0 fail。7 项跳过不计作模型测试成立；最终 provenance 索引及 artifact 上传均成立；artifact `qualification-evidence-5c6ce847fc07329802e3e98db9db70cc683f1f75-30790372286-1`（ID `8846860080`，GitHub ZIP digest `5968779b61db0eb6f6d2d7e7dcaa3d0a38844f8987a43941bb4395aff5ba69ef`）下载后通过五个 strict readers、四份报告与两个源码 digest、跨报告绑定及 41 文件/29 JSON 隐私负扫描；索引绑定 installer SHA=`618f02eddbbd3a956d679b17180817665d48dd0b9608262fd6519f53eac857e0`，下载包不含 installer 字节。该精确 revision 的 J9-CI 已达到联合验收完成，且不关闭 Gate 0B 模型选择或 I2/I3/I4 实机门禁。 |
| **SEM-T04** | 失败路径 | 每项用户能力至少覆盖一个关键失败或降级路径；核心能力必须 fail closed 或显式降级。窗口交互代次同步至少覆盖最小化前缺失 `pointerup/pointercancel/blur`、过期窗口交互代次、renderer reload、指针坐标不可用、1000ms 同步确认超时和原生穿透设置失败；失败时不得继续旧拖动 timer、旧 IPC 或旧 rAF，字幕窗保持隐藏或穿透，工具条同步超时后显式降级为整个 BrowserWindow 实心命中并保留可访问主入口，允许迟到的当前代次合法确认或下一次恢复重试。 | 只覆盖 happy path 不得标记验收完成；固定诊断只能是 `interaction-sync-timeout`、`interaction-pointer-unavailable`、`interaction-pass-through-failed` 或 `stale-interaction-generation`，且只携带 `role/code`，不得携带坐标、设备名、路径或字幕正文。 | 场景矩阵中的故障旅程或实机故障注入。 | SQLite 事实、Gateway 队列、DB2 第二文件中断、产品启动迁移失败清理和退出超时强制终止已有回归；历史层覆盖活动会话拒绝、越权载荷拒绝和取消导出不写文件；模型层覆盖坏 hash/size、越权 host/path、归档 traversal/link、活动会话拒绝替换，以及 fetch/tar 无视 abort 时的有界退出、合法 `.part` 保留与下次启动清 staging。窗口交互代次同步失败矩阵已由 J17/J19 联合旅程与 schema-v7 产品壳确定性证据覆盖，达到联合验收完成；真实任务栏与 DWM 故障观察仍为实现完成·尚未验收。SEM-F29/J23 的 Schema、权限、租约、重试、claim 前后取消、应用中断、utility replacement、renderer reload 与隐私失败矩阵已达到联合验收完成；正式 Agent 的 J7/J21/J22 失败路径仍待实现，向量已后置。 |
| **SEM-T05** | 完成状态词 | 只允许“已决定”“实现完成·尚未验收”“联合验收完成”“实机验收完成”“发布验收完成”。无修饰的“完成”必须能指向所需最高门禁。 | 不允许用“已接通”“可用”“测试通过”替代明确状态。 | PLAN/README 状态与测试报告链接一致。 | 从本次文档起执行。 |
| **SEM-T06** | 场景登记 | 新功能或语义变化必须先在本表和 `testing-strategy.md` 增加/更新旅程，再实现；修复不能只新增孤立单测。 | 未登记的隐藏验收口径、开发完成后再补需求均不接受。 | PR 同时引用 SEM ID 与 J/I/DB gate。 | 规则已生效。 |
| **SEM-T07** | Agent 纪要完成门禁 | 字幕→会后纪要必须分别从 `loopback` 与 `mic` 单路会话验证，并覆盖精修、暂停/恢复、崩溃、Agent 失败和来源互斥；不测试或支持双路并发。 | 只用一段固定文本调用 LLM 的测试不能称纪要功能完成；纪要未完成不影响字幕 MVP 验收。 | J3–J7/J13 + 适用实机证据。 | 已决定；尚无正式 Agent 产品证据，Agent 阶段阻断。 |
| **SEM-T08** | 数据迁移门禁 | JSONL→SQLite 必须可中断、可重跑、可核对，并保留每段最早有效 `final` 作为权威原始转写；遗留 `refined` 必须保持可审计且迁移为可区分的精修版本，不能折叠后遮蔽原始版。原始版与精修版的详情和导出 digest 分别核对。 | 只核对旧“final/refined 当前投影”、只保留最高 revision、或让旧精修稿成为默认原文，都不能满足新迁移边界；仍禁止长期双写。 | DB2 + J10 + J15b。 | 迁移已改取每段**最早**的有效 `final` 参与原文 digest，遗留 `refined` 仍照常导入为可审计的精修版本但不再遮蔽原始版；DB2/J10 的原子、幂等、完整导出与两版 digest 分别核对均已通过。 |
| **SEM-T09** | 向量门禁（后置） | 只有未来启用 `sqlite-vec` 时，才必须通过开发版/打包版加载、旧向量失效、索引重建、扩展缺失降级和不同模型维度隔离。 | 本门禁不得阻断 SQLite 历史、字幕 MVP 或首版 Agent 摘要。 | DB4 + J11 + 对应打包版验收。 | Deferred；近期不执行。 |
| **SEM-T10** | 插件联合门禁 | 每个 Agent 插件必须在真实插件宿主、字幕提交边界、SQLite 产物存储和 UI 读取链路中测试；同时覆盖插件超时/异常/重复触发/卸载及越权请求。 | 直接调用插件函数、mock 掉宿主和数据库或只验证 prompt 文本，不等于插件能力完成。 | J3/J7/J13；A1/A2 PR 阻断。 | 已决定：SEM-F29/J23 的两个静态参考插件已走真实 `AgentPluginHost`、storage worker、候选 SQLite、React UI 与 Pi Loop，该隔离切片已达到联合验收完成；D4/D5 的五个正式插件又以真实 `AgentPluginHost`、storage worker/SQLite、`ModelGateway`、Pi Loop 和 writer 分流覆盖依赖缺失、超时、运行中卸载、Schema 错误与独立失败，D6 再让同一正式后端纵切经过真实 storage utility transport，均为实现完成·尚未验收。正式字幕提交边界、Agent utility、UI 读取链路及完整外部操作越权矩阵尚未闭合。 |
| **SEM-T11** | 模型供应链门禁 | 确定性 CI 必须分别证明核心临时字幕识别器+权威识别器+VAD 的首次供给/离线复启，以及精修模型的独立供给与启用流程：缺失时点开关不发起网络、开关保持关闭并显示下载动作；用户明确下载后沿用固定 manifest、续传、字节/SHA、归档白名单、staging 与原子 ready marker；就绪后仍保持关闭，用户再次明确开启才从下一会话生效。活动会话必须拒绝精修模型下载；允许修改全局精修偏好，但本会话继续使用开始时冻结的值，修改只影响未来会话。取消必须终止网络并保留合法 `.part`，应用重启保持 fetch=0，只有用户明确“继续下载”才以 Range 续传。还必须覆盖跨重启：开启且模型仍 ready 时保留开启；开启后模型缺失/损坏时启动回落关闭并通知、fetch=0；重新下载后仍关闭，直至再次明确开启。 | 旧式一次性 bundle 通过不能证明默认不下载精修、核心独立 ready、两次显式动作、全局偏好、用户取消/继续、跨重启失效回落或可选模型故障降级；下载成功不能冒充用户同意启用，底层能读取 `.part` 也不能冒充产品已有取消入口。 | J14/J15c；真实模型调用 I2/I4。 | 联合验收完成：固定四资源 manifest、三项核心 ready marker、设置页三角色、受控首次供给、packaged 离线复启、精修独立下载/取消/Range 继续、偏好与跨重启复核已闭合。真实模型调用与干净机公网供给继续归 I2/I4。 |
| **SEM-T12** | 打包与发布证据分层 | B5 打包态确定性资格与 I4 干净机发布验收是两个独立门禁。B5 必须绑定精确制品 SHA，并分别证明正式 package layout、受控 packaged 首启/复启、native/SQLite utility、两次 exact-child 退出、同轮 run ID/四份报告 SHA/完整产品载荷 SHA 和 NSIS 生命周期；I4 必须用同一精确安装器在无仓库/Node/既有数据的 Win11 上执行公网、权限、真实音源、离线复启和数据保留。I4 拆为一个无 capture 的 `pass/partial` 子报告、互斥顺序执行的 `loopback`/`mic` 两个音频 child 报告和一份严格 summary；两个音频 child 与 strict summary 必须绑定同一 B5 installer、release layout 和非音频报告摘要。音频 child 分别覆盖权限拒绝/批准、开始、暂停/恢复、停止、历史、导出、离线复启与隐私负扫描；summary 只有在非音频报告及两个音频 child 全部满足各自要求时才能形成完整 I4 结论。 | 任何单份 B5、I4 非音频报告、单来源音频 child 或操作者 attestation 都不得把受控资源/fake ASR/静默安装/断网成功冒充公网流量审计、真实模型推理、另一音频来源、文件/进程/哈希验证或完整发布验收；clean exit 不等于 `0x80000003` 已根因定位。 | B5 七份结构化报告 + I4 非音频 partial report + I4 `loopback`/`mic` 音频 child reports + strict summary。 | 当前确定性资格由 revision `bbfd7041e5963e51942392323735298a7b81cb30` / run `31191838016` 绑定，已达到联合验收完成；远端 installer SHA=`d77d16c0…060c`、产品载荷 SHA=`e95fd87f…a35a`，下载 artifact 不含 installer 字节。历史本地候选 `d862c5fc…0de10` / `a1f03ed6…9accc` 不得作为当前 I4 安装器。I4 非音频 runner/verifier、两个来源隔离的音频 child、strict summary 与移交包入口仍为实现完成·尚未验收；必须另行取得并核验 bbfd/run 的精确 installer 字节，尚无专用干净机三份 child 报告，完整 I4 尚未达到发布验收完成。 |
| **SEM-T13** | 产品路径 I2 证据 | `loopback` 与 `mic` 必须分开执行真实 Electron/audio-host/online ASR/VAD/offline refine；当前权威 series 每来源固定 5 轮形成 P50/P95/min/max。每轮 schema-v5 child report 只有与外部运行器在 exact child `exitCode=0` 且未触发运行器终止后生成的 schema-v1 exit sidecar 按 report SHA 一一绑定，才可进入 schema-v6 series；缺失、多余、重复、错序、错绑或未知字段必须 fail closed。自动 mic fixture 必须先通过同轮 memory-only Gate 0C，绑定精确 preflight SHA、匿名输入/输出 label SHA，并以唯一标签匹配防止预检后静默换端点；其规范名是 `physical-preferred-label-heuristic` 声学 fixture。I2 interaction schema-v2 必须在兼容旧 v1 的前提下增加 `device-removal-retry` 与 `sleep-wake-retry`：runner 只等待操作者执行设备移除/禁用或系统睡眠，必须由真实运行状态分别观察 `AUDIO_TRACK_ENDED`/`SYSTEM_SUSPEND`、capture 已释放、无自动重采集、明确 Retry 后恢复监听、再次出现字幕以及 SQLite/sequence/transport 边界；completion 只证明操作者执行动作，不能单独产生 `pass`。整个 bundle 只能写指标、哈希和固定语义，不写正文、PCM、音频文件或路径；原始证据 JSON 必须在对象校验前拒绝非法 UTF-8、BOM、重复键、非有限数值与尾随输入，再执行闭合字段验证；语料 WAV 本地生成且忽略，报告绑定其 digest 与受跟踪 reference digest。 | label heuristic 不是硬件证明，不能排除未知或伪造标签的虚拟设备；exit sidecar 只防止内部 `pass` 后悬挂/超时或被运行器终止的误绿，不是签名、远端执行、硬件身份、崩溃根因证明；操作者 completion 也不能替代产品状态证据。VB-Cable、系统默认输入、单次通过、模型裸 benchmark 或托管 CI 都不能冒充更强的 mic 结论；5 轮 happy-path 也不等于性能、拖动、设备、睡眠、I3 或 I4 验收完成。 | 权威 bundle `docs/validation/i2-live-v5/`：Gate 0C SHA-256 `0f9f7668751c64fbce922883421ead41680226126800e0b7f6b3da81b39840ef` + 两来源各 5 个 schema-v5 child、5 个 schema-v1 exit sidecar 与 1 份 schema-v6 series；`device-removal-retry` 与 `sleep-wake-retry` 使用兼容 v1 的 schema-v2 独立报告，`dwm-drag` 使用兼容历史 schema-v3/schema-v4 且当前候选矩阵只接收 v5 的 schema-v5 report/completion。 | 受跟踪的 2026-07-31 两来源各 5/5，冻结 P95=1158/1005ms；2026-08-01 当前机复跑仍各 5/5 结构/准确率/自然退出/零损失，冻结 P95=1148/1099ms。revision `b96b8fe7db5ba4db3ac36c4ee85371a4381b521f` 的新 `loopback` 五轮也闭合结构、真实精修、自然退出与零损失，series SHA-256=`2a365e3c6a1075336b9c7df65ad5b3ca36094a991d5b68532d15e65556ab1b48`，冻结 P95=1242ms；三批均未满足 `<1000ms`。真实 pause/refine 与 exact worker 强制终止+Retry 已通过；原生拖动、实际设备移除、睡眠/唤醒仍待。兼容 v1 的 schema-v2 runner 与 schema-v5 `dwm-drag` runner、strict verifier、进度及操作者 completion 协议现为实现完成·尚未验收；completion 单独存在、缺失真实故障或缺少任一恢复边界都不能产生 `pass`。尚无同 revision `mic` 五轮、两个新恢复场景或当前候选 12 组合 DWM 实机报告，因此 I2 整体仍为实现完成·尚未验收。 |
| **SEM-T14** | 首字延迟观测边界 | 冻结字幕可见延迟是唯一 I2 首字验收值，必须从冻结语料 `source t0 + 140ms` 固定 onset 算到 `SessionCoordinator` 接受并通知观察者的同一个首个 partial。现场运行必须先完成 playback/audio-host/utility 的固定样本时钟校准，再预留同一个未来 `source t0`，先按该时刻 arm 捕获诊断、再按该时刻 schedule 播放，不能让校准或 IPC 偷吃 140ms onset 预算。schema-v5 child 可用 NTP 式最小 RTT 校准，把冻结值拆成六个非负整数区段；同一 child 的六段必须精确望远镜求和，校准必须有固定 clock ID、样本数、RTT/不确定度/年龄上限。captured energy onset 只作阈值诊断，并固定从 `source t0 + 40ms` 的 post-source guard 后观察；该 guard 仅排除前置环境声/亚毫秒跨时钟不确定区，仍比冻结 onset 早 100ms。 | 不得把 40ms guard、captured onset、VAD 事件、worker publish、DOM paint 或各区段单独替代冻结端到端门槛；不得把各轮区段 P95 相加冒充某轮总延迟；不得 clamp 因果倒置、移动 `source t0 + 140ms`、改变 onset 规则或用 guard 制造达标。mic 的负 `capturedOnsetMinusFrozenEstimateMs` 只表示 guard 后已有环境能量；它既不证明语料/说话人起音，也不能降低冻结延迟。 | 任意远端时钟原点/错误 ID/高 RTT/陈旧校准/因果倒置纯测试；校准先于 shared-t0 arm/schedule 的延迟 IPC 测试；audio-host→worker→adapter→Coordinator 的 exact accepted-partial 组合测试；schema-v5 两来源各 5 轮实机证据。 | 六段链路及严格 schema 已实现并通过纯测试、组合测试和 5+5 实机取证。既有 series P95 依次为 loopback 729/405/1/33/1/1ms，mic 557/500/1/28/1/1ms；revision `b96b8fe7db5ba4db3ac36c4ee85371a4381b521f` 的新 `loopback` 六段 P95 为 814/400/1/28/1/1ms。这些诊断显示主要预算位于采集/VAD 与触发所需音频，而不是 IPC，但不得把各列相加或替代冻结值。新 `loopback` 冻结 P95=1242ms，既有 `mic` 冻结 P95=1005ms，均未被 captured-energy 或分段诊断改判通过。 |
| **SEM-T15** | 正式 Agent 首版门禁 | 正式 Agent 首版必须同时闭合字幕系统独立降级、SEM-T07 的 `mic`/`loopback` 单路纪要组合、正式插件权限、识别 provider、Agent 模型 provider 的配置、凭据、冻结和降级、确认关键词、三项后台 Agent 任务、个人记忆、调试聊天及正常使用边界组合。其中至少包含 registry、main-only 受信任非敏感配置表、早于所有窗口与子进程读取并清除的 `DEEPSEEK_API_KEY`、冻结 provider/model/预算、exact DeepSeek origin、Agent 处理资格与显式降级；真实 DeepSeek 公网、账户、配额和模型质量后置。确定性联合测试保留真实 storage worker、SQLite migration、PluginHost、job runner、preload/IPC 与 renderer，只替代 Agent 模型 provider、云网络、启动环境凭据、声卡和系统权限。Agent 处理资格、空/短/超上下文输入、重复动作、丢失回复、退出恢复、取消、错误分类、设置变化、会话删除、本地任务让行、多会话排队、插件隔离、可访问状态和隐私负扫描均为阻断项。 | 不得用 SEM-F29/J23 隔离入口、设计追踪正则测试、直接插件调用、内存 repository、单一顺利路径、单一 renderer snapshot 或一次模型请求冒充正式产品；不得因真实 provider 未接入而让测试直接提交产物或伪造 `ready`；不得因部分 Agent 失败降低 J1/J2/J10/J12 字幕能力。 | SEM-T07/J3–J7 + J12/J13/J20/J21/J22/J24 + DB7；正式 package layout 另分层，真实 DeepSeek 接入后再建立供应商/公网证据。 | 已决定；J24 组合矩阵与接口合同已登记，D3–D6 的正式存储、三项任务 UI-free 后端与 storage utility transport，以及 D9–D12 的 provider bootstrap/registry、设置存储和字幕提交边界接线子边界均为实现完成·尚未验收；D13 正式任务存储网关接线已登记。正式 Agent utility、main/preload/renderer、provider 凭据启动接线、确认关键词、调试聊天、用户读取/操作链路、`mic`/`loopback` 组合与实机证据仍未闭合。 |

> **SEM-F28 / SEM-T15 的 D9 启动凭据精确化**：正式 main 取得启动环境中大小写不敏感等价于 `DEEPSEEK_API_KEY` 的 raw 项后，必须先无条件删除全部等价键，再校验配置表与凭据；多个大小写等价键属于歧义输入并投影为 `credential_unavailable`。子进程环境只能从删除后的启动快照复制，运行中重新注入环境变量既不得改变凭据状态，也不得进入后来创建的 child、worker 或 utility。该规则取代表内“校验、复制并删除”的顺序简写；D9 只实现 main-only bootstrap/catalog 子边界，不表示正式 main 已接线、Agent utility 已取得凭据或真实 DeepSeek 已接入。
> 当前 D9 子边界为实现完成·尚未验收：闭合配置表、公开状态、非敏感 Agent 处理资格事实、净化后的 child environment 和有界凭据借用已由一条真实 `StorageWorkerService`/`FormalAgentStore`/SQLite 联合旅程覆盖；D9 自身不包含正式 `process.env` 首行接线、真实 child/Agent utility、ConfigStore v2、preload/renderer、HTTP 或完整 J24，后续子边界证据另按各批次记录。
> **SEM-F25 / SEM-F28 / SEM-T15 的 D10 registry 子边界**：正式 `ModelGateway` 只能接受项目自有 `AgentModelProviderRegistry` 实例，并经该实例解析任务已冻结的 provider/model/预算与超时；不得让业务插件或确定性测试替身以 duck typing 直接充当 registry。registry 只接受与 D9 启动快照精确匹配的第一方适配器描述；它交给 `ModelGateway` 的模型句柄必须是冻结的 exact `{ model, streamFn }`，不得携带 `apiKey`、credential 或任意额外字段。一次 `withModel(request, signal, consumeModel)` 借用必须覆盖从打开模型句柄到 Pi Agent Loop 完整收束的同一调用；同一个受超时约束的取消信号必须传入模型打开与 Loop，成功、异常、取消或超时后均释放并尽力清零凭据副本。稳定鉴权失败还必须使 D9 主凭据失效；408/429/网络/5xx、取消和无效结构化输出不得误清除主凭据。D10 仍不实现真实 DeepSeek HTTP、正式 main 或 Agent utility，不得据此声称供应商公网或完整 J24 已验收。
> 当前 D10 registry 子边界为实现完成·尚未验收：正式纪要、三项后台 Agent 任务与 storage utility transport 旅程已统一经过项目自有 `AgentModelProviderRegistry → ModelGateway → Pi Agent Loop`，覆盖 exact 模型句柄、配置预算/超时、模型打开阶段取消、408 重试、无效输出及稳定鉴权失效；真实 DeepSeek HTTP、正式 main/Agent utility、preload/renderer 与完整 J21/J24 仍未形成产品证据。
> **SEM-F26 / SEM-F28 / SEM-T15 的 D11 ConfigStore v2 子边界**：当前平面 `ConfigStore` 必须从 schema v1 迁移到 v2，并以 exact 平面字段保存 `agentEnabled=false`、`automaticProcessingSince=null`、`memoryEnabled=true`、`memoryProcessingSince=null`、`cloudDisclosureAccepted=false`、`agentSettingsRevision=0`。v1 迁移必须保留合法字幕设置和已完成的单路来源选择；v2 只有六个 Agent 字段全部合法且满足 `(automaticProcessingSince !== null) === agentEnabled`、`(memoryProcessingSince !== null) === (agentEnabled && memoryEnabled)` 才可保留，否则只把六个 Agent 字段整体回落到首次默认值，不得污染字幕设置。正式 Agent 设置更新只接受 exact `{ expectedRevision, agentEnabled, memoryEnabled, cloudDisclosureAccepted }`：revision 不匹配返回 `SETTINGS_REVISION_CONFLICT` 且配置文件零写入；匹配时，Agent 总开关或个人记忆每次从不生效转为生效都以受信任当前时刻建立新边界，保持生效则保留旧边界，任一不生效则对应边界为 `null`，并在同一次原子替换中把 revision 恰好加一。通用配置 patch 不得写 schema 或上述六个 Agent 字段，更不得写 provider、URL、model 或凭据。D11 只闭合 main-owned 配置存储与真实 storage worker/SQLite 资格组合，不接正式 main IPC、preload、renderer、Agent utility 或 DeepSeek 公网。
> 当前 D11 ConfigStore v2 子边界为实现完成·尚未验收：真实 `ConfigStore → AgentProviderBootstrap → StorageWorkerService/FormalAgentStore → SQLite` 联合旅程已覆盖 v1 持久迁移与重启、六项默认值及合法/损坏 v2、revision 冲突零写入、Agent 与个人记忆自动处理边界、三项后台 Agent 任务共享冻结输入、关闭/重新开启时的取消和不补处理，以及配置文件/SQLite 的凭据与 provider URL 负扫描。正式 main/IPC、preload、renderer、Agent utility、真实 DeepSeek HTTP 与完整 J24 仍未形成产品证据。
> **SEM-F00 / SEM-F12 / SEM-F28 / SEM-T15 的 D12 字幕提交边界接线**：main-owned `FormalAgentRuntime` 必须只从 ConfigStore v2 当前 Agent 设置、D9 `AgentProviderBootstrap` 非敏感资格事实和受信任本地模型就绪布尔值构造 exact `AgentEligibilityContext`，不得接受 renderer、插件或 Agent 模型 provider 覆盖，也不得携带 URL、预算或凭据。`MeetingStoppedPersistenceSink` 只能在底层 `SqliteSessionRecorder.closeSession` 或其终态重试取得持久化确认后，向 `AgentJobReconciler` 投递仅含 `sessionId` 的尽力通知；通知和对账不得改变或延迟字幕 `stop` 回执，不得阻止下一会话开始，也不得参与字幕事件事务。相同会话的并发通知在内存中合并，storage worker 仍以 SQLite dedupe identity 决定最终幂等；通知失败只发布不含会话正文、凭据、路径或原始 Error 的稳定诊断，随后由启动扫描或 storage worker replacement 后的显式恢复先重放当前任务策略、再对终态会话补建。`StorageGateway` 必须把任务策略确认绑定到当前 active storage worker 代次；replacement、关闭或强制终止都会清除该确认，当前代次未确认策略时对账必须 fail closed，不能把旧代次的 ready 状态带入新 child。Agent 关闭、自动处理边界之外、凭据不可用或零条首次稳定转写均不创建任务；恢复扫描不得补处理边界之前的会话，也不得复活已取消任务。D12 复用并升级 D6 的正式 utility-process 联合旅程，不新增同义低层测试；不修改正式 main/preload/renderer，不创建 Agent utility，也不接真实 DeepSeek HTTP。
>
> 当前 D12 字幕提交边界接线子边界为实现完成·尚未验收：UI-free `ConfigStore → FormalAgentRuntime → SessionCoordinator → MeetingStoppedPersistenceSink → SqliteSessionRecorder → StorageGateway → StorageWorkerHost/StorageWorkerService → SQLite` 纵切已经形成，既有 D6 旅程以真实 exact-child 强制终止和 replacement 覆盖持久 close 后尽力通知、下一会话先开始、当前代次策略先行、显式恢复、同会话通知合并、零条首次稳定转写不建任务和同 `runId` 恢复。正式 main/preload/renderer、Agent utility、Agent job runner 的完整 `StorageGateway` 路由、真实 DeepSeek HTTP 与完整 J24 仍未形成产品证据。
>
> **SEM-F09 / SEM-F12 / SEM-F28 / SEM-T15 的 D13 正式任务存储网关接线**：正式 `AgentJobRunner`、`TranscriptReader`、`MemoryReader`、`ArtifactWriter` 与 `MemoryCandidateSink` 必须共享 main-owned `StorageGateway`，不得取得或缓存具体 `StorageWorkerHost` 代次。网关必须为冻结字幕读取和个人记忆读取提供与既有 Agent 命令相同的串行、克隆和确定性业务拒绝隔离边界；claim、续租、重试、取消与两类提交继续以原幂等身份跨 replacement 重放。每次 runner 开始领取前，当前 storage worker 代次必须已经确认当前任务策略；replacement 清除该确认，恢复入口先重放策略再继续领取。若 Agent 模型 provider 已返回、产物提交前 exact storage child 退出，字幕事实与已冻结 `runId` 不变，网关只能在确认旧 child 退出后创建 replacement，并以同一提交身份收束为至多一个产物；不得让 Agent 业务拒绝熔断字幕事实 FIFO。D13 复用 D6/D12 的 utility-process 联合旅程，不新增同义测试；仍不创建 Agent utility，不接正式 main/preload/renderer 或真实 DeepSeek HTTP。
>
> 当前 D13 正式任务存储网关接线为已决定；先实现上述 UI-free 子边界并保持 `agentUtilityProcess=false`，不得据此提高完整 J21/J24 状态。

> SEM-T13 的 offline refine 证据组合与时序补充：I2 child 只有解析到受批准精修模型后，才可在会话配置冻结 `refinementEnabled=true` 并在运行时能力冻结 `refinementAvailable=true`；模型缺失必须 fail closed，报告中的模型 ID 不能单独证明真实精修链路已启用。启用精修的 child 在固定尾静音窗口结束、首次稳定转写已出现但精修稿尚未出现时，最多额外等待固定 15,000ms 的真实精修结果；该上限必须使用单调时钟。精修稿一到立即继续 Stop；一旦观察窗口超时，该事实必须冻结，随后 Stop 冲刷期间到达的迟到精修稿也不得把本轮改判为合格，仍以 `refined-caption-missing` fail closed。该观察窗口只服务真实精修证据，不得改变冻结字幕可见延迟的 `source t0 + 140ms` 起点、首个已接受 partial 终点或 `<1000ms` 门槛。双重冻结、模型缺失失败、有界早到/超时行为及 `refined-caption-missing` 枚举现为实现完成·尚未验收；仍须由绑定精确 revision 的新五轮实机 series 验收。
>
> 2026-08-03 的 revision `b96b8fe7db5ba4db3ac36c4ee85371a4381b521f` 新 `loopback` series 固定 5 轮，schema-v5 child、schema-v1 exact-child exit sidecar 与 schema-v6 summary 的 strict reconstruction 返回 `pass`；series SHA-256 为 `2a365e3c6a1075336b9c7df65ad5b3ca36094a991d5b68532d15e65556ab1b48`。5 轮均有首次稳定转写和精修稿，final/refined CER 最大值为 0，全部 transport 损失轴最大值为 0，隐私字段均为 false；冻结字幕可见延迟 P50/P95/min/max 为 1144/1242/1054/1242ms，仍不满足 `<1000ms`。六段 P95 为 814/400/1/28/1/1ms，只用于定位采集/VAD 与触发所需音频预算，不得把各列相加或改判门槛。该证据关闭了本轮 I2 runner 精修组合回归，但没有新的 `mic`、原生拖动、实际设备移除、睡眠/唤醒或 I3 证据，因此 I2 整体仍为实现完成·尚未验收。

> 2026-08-03 的历史 I2 模型替换决策（SEM-F01/SEM-T14/J1）：对上述五个 exact child 逐轮复核后，`audioNeededAfterCapturedOnsetMs` 为 712.625–776.562ms；最慢轮冻结字幕可见延迟为 1242ms，触发后的四段发布/路由合计 28ms；**仅在保持该轮已观察到的** 437.438ms 采集/VAD 前置和 28ms 触发后组合时，要保持冻结 `<1000ms` 线，实时模型音频需求必须低于 534.562ms。Gate 0B 同一 `zh-en-code-switch` 冻结语料的 `x-asr-160ms` 裸模型证据为首个临时字幕 P95=697.4775ms、观测最大音频需求 660ms。该比较不是物理下限证明，不排除另行登记的捕获拓扑改造或新证据改变工程取舍；它只说明当时证据没有为继续参数微调该候选提供达线依据。**已决定**停止该轮参数微调并重新开启 Gate 0B 实时模型替换评估；尚未选定替代模型，既有候选当时只保留为实现基线。替代候选必须先按同一冻结语料、质量、RTF 与冻结字幕可见延迟口径登记并复测，再重新执行 `loopback`/`mic` 各五轮 I2。不得移动 `source t0 + 140ms`、降低 `<1000ms`、缩短 Silero 冻结参数或把诊断区段改作验收值；因此 I2 仍为实现完成·尚未验收。
>
> 2026-08-03 的历史 Gate 0B 替代候选登记（SEM-F17/SEM-T11/SEM-T14/J1/J14）：**已决定**只把官方 `k2-fsa/sherpa-onnx` `asr-models` 资产 `sherpa-onnx-streaming-zipformer-bilingual-zh-en-2023-02-20.tar.bz2` 登记为 `evaluation-only` realtime 候选，不表示已批准替换当时生产使用的 `x-asr-160ms`。官方 URL 固定为 `https://github.com/k2-fsa/sherpa-onnx/releases/download/asr-models/sherpa-onnx-streaming-zipformer-bilingual-zh-en-2023-02-20.tar.bz2`，归档字节数为 `511274346`，本机从该 URL 取得的 SHA-256 为 `27ffbd9ee24ad186d99acc2f6354d7992b27bcab490812510665fa8f9389c5f8`；评测只使用顶层 int8 encoder/joiner、decoder 与 `tokens.txt`，不得把归档内 fp32 文件静默算入生产 allowlist。候选必须先在同一四条冻结语料上完成官方 CLI RTF、内容质量与 Node N-API 五轮 40ms 喂帧首个临时字幕复测；内容质量按当时生产受控基线固定为四例 macro CER 不高于 `0`、英文例 WER 不高于 `0`，实时模型标点仍由用户明确启用的可选精修补齐，不作为本次替换门槛。报告还必须把 `zh-en-code-switch` 的观测音频需求与条件性 `534.562ms` 工程筛选值并列；该筛选值不是 I2 验收门槛，也不得替代冻结字幕可见延迟。候选若未同时保住内容质量、批准机器基线 CLI RTF `<0.60` 与四例首个临时字幕 P95 `<1000ms`，直接保持 `evaluation-only`；若进入生产选择，必须另行修改固定 manifest 的 URL/字节/SHA/运行文件白名单并重跑 J14 核心字幕模型资源包首次供给、Range 续传、ready marker、离线复启及 `loopback`/`mic` 各五轮 I2。当时的生产 manifest、核心下载预算和 ready marker 均保持不变。
>
> 该 large bilingual Zipformer 的最终 registry 绑定同机实测已经形成内容脱敏投影：官方 CLI 四例 max RTF=`0.17`，Node N-API 四例首个临时字幕 P95 最大值=`630.5279ms`，`zh-en-code-switch` 观测音频需求最大值=`300ms`；但 macro CER=`0.05998168498168498`、英文 WER=`0.1111111111111111`，均高于当时生产受控基线 `0`。因此**已决定**该候选继续保持 `evaluation-only`，不进入生产选择；当时的生产 manifest 与 J14 供应链不变。
>
> 2026-08-03 的第二个历史 Gate 0B 替代候选登记（SEM-F17/SEM-T11/SEM-T14/J1/J14）：**已决定**把官方同仓库 `asr-models` 资产 `sherpa-onnx-streaming-paraformer-bilingual-zh-en.tar.bz2` 登记为另一个 `evaluation-only` realtime 候选。官方 URL 固定为 `https://github.com/k2-fsa/sherpa-onnx/releases/download/asr-models/sherpa-onnx-streaming-paraformer-bilingual-zh-en.tar.bz2`，HEAD 与完整下载字节数均为 `1047319737`，本机完整文件 SHA-256=`5462a1fce42693deae572af1e8c4687124b12aa85fe61ff4d3168bb5280e205f`；评测运行文件只允许 `encoder.int8.onnx`、`decoder.int8.onnx` 与 `tokens.txt`，不得把同归档 fp32 encoder/decoder 或上游 WAV 解入模型目录。该候选沿用上一段全部冻结语料、质量、CLI RTF、五轮 40ms 首个临时字幕与条件筛选边界；其归档相对当时生产实时模型增加 `913421730` bytes，只要尚未通过候选门禁，当时的生产 manifest、J14 下载预算、ready marker 与运行时均不得改变。即使候选门禁成立，也只能进入后续生产选择，必须另行登记并重跑完整 J14 与两来源各五轮 I2 后才能改判。
>
> 该 bilingual Paraformer 的最终 registry 绑定同机实测同样形成内容脱敏投影：官方 CLI 四例 max RTF=`0.29`，Node N-API 四例首个临时字幕 P95 最大值=`588.0249ms`，`zh-en-code-switch` 观测音频需求最大值=`500ms`；但 macro CER=`0.024267399267399264`、英文 WER=`0.4444444444444444`。因此**已决定**该候选继续保持 `evaluation-only`，不进入生产选择。官方在线模型目录中当时尚未实测且仍覆盖中英的只剩额外覆盖粤语的 streaming Paraformer trilingual；**已决定**把官方资产 `sherpa-onnx-streaming-paraformer-trilingual-zh-cantonese-en.tar.bz2` 登记为最后一个该轮 `evaluation-only` 候选。其固定 URL 为 `https://github.com/k2-fsa/sherpa-onnx/releases/download/asr-models/sherpa-onnx-streaming-paraformer-trilingual-zh-cantonese-en.tar.bz2`，HEAD 与完整下载字节数均为 `1047671211`，完整文件 SHA-256=`d479167d8752628d9032d29de1060493865389d1e295a1c2e8e011e7062f1932`；只允许解出 `encoder.int8.onnx`、`decoder.int8.onnx` 与 `tokens.txt`，归档相对当时生产实时模型增加 `913773204` bytes，沿用完全相同的冻结候选门禁。中文-only、英文-only 与 online CTC 中文模型不满足字幕 MVP 同一会话中英内容边界，不进入该轮候选登记。
>
> 该 trilingual Paraformer 的同机实测结果为官方 CLI 四例 max RTF=`0.27`、Node N-API 四例首个临时字幕 P95 最大值=`575.8275ms`、`zh-en-code-switch` 观测音频需求最大值=`500ms`，但 macro CER=`0.21543040293040294`、英文 WER=`0.3333333333333333`，仍未保住当时生产受控基线。因此**已决定**该轮三个新登记候选全部保持 `evaluation-only`，未选定替代实时模型；历史 small bilingual 质量失败、当时 `x-asr-160ms` 产品链路延迟未达线以及该轮 large/两项 Paraformer 的脱敏指标共同关闭当时 sherpa-onnx runtime 可直接部署的官方在线中英候选矩阵。严格汇总 [`docs/validation/gate-0b-realtime-candidate-summary.json`](validation/gate-0b-realtime-candidate-summary.json) 绑定 registry SHA-256=`d202c018aa295e5d1859765c1856dab7509280ad7dfcf96c601e1657b48bdcac`，自身 SHA-256=`682d71b5bb8ff7851ceec15e0673bf52943cc74ca9611f9617306d18c4c08a1f`，重建为 `no-eligible-candidate`、0 个 eligible ID、未选定替代且当时生产 manifest 未变。当时的生产 manifest、J14 下载预算、ready marker 与运行时继续使用该历史实现基线且不得改判为 I2 达线；下一步必须另行登记新的模型资产或识别架构，再按相同门槛复测，不能降低 `<1000ms`、移动 `source t0 + 140ms` 或放宽内容质量基线。I2 整体仍为实现完成·尚未验收。
>
> 2026-08-03 的 I3 修复代码候选 T03/J9-CI 证据由 run `30784483976` 精确绑定 revision `82d56f64c80c74f30c1944665460f1316f1d7939`；artifact ID `8844827701`、GitHub ZIP digest `78227ef5b4af08f3f2319156cb4ef096a132599707446506c32124f0ecbcdaca`。Electron `43.2.0` 前置、全部 workflow steps 与 627 项回归返回 `success`；下载侧五个 strict readers、四份报告与 lockfile/workflow SHA、跨报告绑定以及 41 文件/29 JSON 隐私负扫描均闭合。下载包不含 installer 字节，同轮 workflow 在索引生成前复核远端 installer SHA `bdac65edc7541070b9e2b4af13550b5768ff0bc86b4048b13fd6e7aca7dea7c4`。该精确 revision 的 J9-CI 已达到联合验收完成；此记录不扩大 T03 的托管 CI 边界，也不替代 I2/I3/I4 实机门禁。
>
> 2026-08-03 的 I3 live provenance LF 修复候选 T03/J9-CI 证据由 run `30787209338` 精确绑定 revision `f86ac1ef604dc7da0728c6eda44d59bbfd1e09bf`；artifact ID `8845725648`、GitHub ZIP digest `b9d0a56b55fa4c6728be660698654bce418ee6364fd43a59eb1be9ccb9993242`。Electron `43.2.0` 前置、全部 workflow steps 与 629 项回归返回 `success`；下载侧五个 strict readers、四份报告与两个源码 digest、跨报告绑定以及 41 文件/29 JSON 隐私负扫描均闭合。下载包不含 installer 字节，同轮索引绑定 installer SHA `b9becb191234cefa6ddfba48bc2865379d6dc62f1a7020c85124df02f2516f31`。该精确 revision 的 J9-CI 已达到联合验收完成；此记录只关闭 c36 的 checkout 可移植性缺口，不扩大 T03 的托管 CI 边界，也不替代 I2/I3/I4 实机门禁。

> 2026-08-03 的最新 I2 模型替换决策证据投影由 run `30790372286` 精确绑定 revision `5c6ce847fc07329802e3e98db9db70cc683f1f75`；artifact ID `8846860080`、GitHub ZIP digest `5968779b61db0eb6f6d2d7e7dcaa3d0a38844f8987a43941bb4395aff5ba69ef`。Electron `43.2.0` 前置，workflow 结论为 `success`；core 414 tests=407 pass+7 expected model/Silero-asset skips，integration 27/27，evidence 190/190；总计 631 tests=624 pass+7 expected skips+0 fail。7 项跳过不计作模型测试成立；下载侧五个 strict readers、四份报告与两个源码 digest、跨报告绑定以及 41 文件/29 JSON 隐私负扫描均闭合。下载包不含 installer 字节，同轮索引绑定 installer SHA `618f02eddbbd3a956d679b17180817665d48dd0b9608262fd6519f53eac857e0`。该精确 revision 的 J9-CI 已达到联合验收完成；此记录不表示 Gate 0B 已选定替代模型，也不替代 I2/I3/I4 实机门禁。

> SEM-F04/J8 的 I3 实机资格补充：I3 runner 若解析到受批准精修模型并据此要求 `refinedObservedWhenEnabled`，必须同时在会话配置冻结 `refinementEnabled=true`、在运行时能力冻结 `refinementAvailable=true`；只把模型对象传给 adapter 不构成精修已启用的证据。2026-08-03 revision `efccfeda4cb66ff74da23c747f6c4af3495b9659` 的 75 秒 `loopback` qualification 生成 `fail/partial`：墙钟 76,856.073ms、33 个首次稳定转写、worker 强制退出/Retry、SQLite/导出/transport/隐私等其余 14 项检查均成立，但精修稿为 0，唯一失败项是 `refinedObservedWhenEnabled=false`，该报告不得进入 I3 合格证据。双重冻结修复随后由 revision `82d56f64c80c74f30c1944665460f1316f1d7939` 的 75 秒 `loopback` qualification 验证：墙钟 75,540.785ms，恢复前/后/总计 14/17/31 个首次稳定转写、29 个精修稿，15/15 检查均成立，报告 SHA-256=`0c219b9627618cdda12ad41ae77093fd5f7bcccbe30b042c1c9cad2958d702f4` 并通过 strict verifier 与隐私负扫描。该报告是 `pass/partial` 资格证据，只关闭 runner 精修组合缺口，不证明原生拖动、7,200 秒或 3,000 段；不得降低精修检查、25/12/8 段门槛或两小时验收口径，I3 整体保持实现完成·尚未验收。

> SEM-F14 的报告边界同样适用于历史测试证据：受跟踪的 validation JSON 只能保留指标、枚举和哈希，不得以“测试语料”或“旧格式”为由保留字幕正文、可重建正文的 token 数组或音频文件名。被新证据替代的旧报告可保留原文件哈希与迁移计数，但不得继续冒充当前执行结果。Gate 0B 复算所需的正文中间件只允许写入固定、被 Git 忽略且无 symlink/junction 的 `models/gate-0b/private/`；CLI 原始输出只在内存中参与解析和哈希，不提供落盘参数，线程 sweep/流式 benchmark 的普通文件与 stdout 必须先投影为 ID、指标和哈希，任意其他正文落盘路径必须 fail closed。
>
> SEM-F20 的“永久淘汰”也覆盖 reducer 的内存保险上限：该上限严格高于最大视觉容量；一旦旧前缀因保险上限移除，实时 fold 与同会话水合必须同步写入仅含 `segmentId` 的显示墓碑，并以高于当前水位的迟到 `final` 验证 live、canonical replacement 与 reload 均不复活。
>
> ADR 0004 解释规则：上表历史状态中出现的 `final/refined`、三资源 bundle 或 offline refine 只描述旧候选证据。新的默认原始路径必须在不启用精修时独立通过；SEM-T13 的 offline refine 要求仅适用于用户明确启用的可选精修变体，不能重新成为核心字幕 ready 条件。

> 2026-08-07 当前确定性资格：revision `bbfd7041e5963e51942392323735298a7b81cb30` / run `31191838016` 已达到联合验收完成。core 422 tests=415 pass+7 expected model/Silero-asset skips、integration 29/29、evidence 204/204；总计 655 tests=648 pass+7 expected skips+0 fail。artifact ID `8999273285`、ZIP digest `5ce4070c…55af`、installer SHA `d77d16c0…060c`、产品载荷 SHA `e95fd87f…a35a`。schema-v4 产品壳及 packaged 首启/复启闭合三项核心 marker / 四项总资源；本轮未执行采集、播放、WAV 推理或模型推理，不改变 J15a/I2/I3/I4 实机边界。

## 3. 变更维护规则

1. 需求、实现或测试 PR 必须引用至少一个 `SEM-*`；没有适用项时先新增语义行。
2. 改“冻结语义”时必须更新对应 J/DB/I gate；只改“当前状态”不改变要求。
3. 旅程通过后在测试策略和 PLAN 更新证据，但不得删除失败路径或缩小边界来制造通过。
4. “当前状态”至少在阶段验收、存储后端切换、摘要上线和发布前复核一次。
5. 模糊词按 [`CONTEXT.md`](../CONTEXT.md) 替换；特别禁止用“本地声音”“最终字幕”“单测通过所以完成”。
